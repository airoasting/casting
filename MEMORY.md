# MEMORY.md — 에이전트 팀 빌더 변경 로그

> 규칙: **변경할 때마다 아래 "변경 로그" 맨 위에 한 줄 추가**한다. 형식 `YYYY-MM-DD · v{N} · 무엇을/왜`. 과거 기록은 지우지 않는다.

## 변경 로그
- 2026-06-28 · v57 + router.js 확장 · **팀 빌더에 더 많은 팀 구조 + 다이내믹 워크플로우**(사용자 /goal: "다이내믹 워크플로어로 다양한 팀 구조, skill-creator로 10점"). router.js: 레시피 12→**28**(법무·약관/보도자료·PR/인사평가/콘텐츠·SNS 등 추가) + 카테고리 4→5("생산성·운영") + **`composeTeam(goal)` 다이내믹 조합 함수**(매칭 레시피 없을 때 출력타입·분석·입력 키워드로 즉석 팀 합성, 복합목적 분석 최대2). 빌더(v57) `renderRecos`: 매칭 있으면 레시피, 없으면 composeTeam(중복 제거) + rankHarness 동률 타이브레이커(전문 레시피 우선). 빌더가 HARNESS/HARNESS_CATS 동적 렌더라 router.js만 늘려도 웹에 즉시 반영(카테고리 칩 5·매칭 28). skill-creator 평가 루프: 독립 적대 리뷰 5.4→(5결함 수정: compose붕괴·출력타입·오라우팅·도메인공백·이원화)→9.3→(compose 다중분석·타이브레이커·고객노이즈)→~9.5, 회귀 0. 동기화: router.js 루트·빌드 / v57→두 index.html / harnesses.md(28) 재생성·skill 로컬·github push(6411f99→029211f). (동시 세션이 아바타로 v35~v56 진행 중이라 v57은 v56 기반.)
- 2026-06-28 · v56 · **히어로 eyebrow 배지 핑크→녹색**(사용자 요청). 544행 `.eyebrow{background:var(--pink)!important…}` → `var(--green)`. 검정 글자 유지(녹색 위 가독성 OK). 팔레트색만 사용. v55→v56, 루트+빌드 동기. 1280px 스크린샷 확인.
- 2026-06-28 · v55 · **히어로 카피 2건 정리**(사용자 요청). ① 서브카피 주술 호응·세련화: "목적을 입력하면 팀장이 검증된 50명 중에서 팀원을 뽑아, 워크플로우까지 짜인 팀을 설계합니다." → "목적을 입력하면, 팀장이 검증된 50명 중에서 필요한 팀원을 골라 워크플로우까지 짜인 팀을 설계합니다."(주어 팀장이 → 골라 → 설계합니다로 호응 일치, 어색한 ", 팀원을 뽑아," 제거, 볼드 범위 `팀장이 검증된 50명`→`검증된 50명`). ② h1에 줄바꿈: "목표를 설정하면, 자동으로 에이전트 팀이 꾸려집니다" → `목표를 설정하면,<br>자동으로 …`("자동으로" 앞 강제 개행). v54→v55, 루트+빌드 동기. 데스크톱 1280px 스크린샷으로 h1 2줄·서브 한 줄 확인.
- 2026-06-28 · v54 · **히어로 우상단 장식을 레이어드 컬러 박스로 교체**(사용자 요청, 참고 이미지=쌓인 카드 느낌). 기존 `.hero-deco` 2×2 4색 그리드(rotate 9deg) → 절대배치 레이어드 박스 3장: `.d-back`(옐로 114px, rotate 4deg, 우하 오프셋) → `.d-front`(핑크 118px, rotate -3deg, 좌상) → `.d-card`(흰 78×54px, `border-bottom:12px` 핑크 띠, rotate -7deg) — 모니터/카드 모티프. 컨테이너 150×144px, `z-index:-1`(제목 텍스트 뒤), `transform-origin:top right`. 참고 이미지의 주황은 팔레트 외라 옐로로 대체(새 hex·그라데이션 금지 규칙 준수). 반응형: ≤1000px `transform:scale(.82)`, ≤760px 숨김. HTML `<i>`×4 → `<i class="d-back/d-front/d-card">`×3. v53→v54, 루트+빌드 동기. 데스크톱 1280px 스크린샷으로 박스·서브카피 한 줄 확인.
- 2026-06-28 · v53 · **히어로 서브카피 한 줄로**(사용자 요청). 880행 `.hero-sub`("목적을 입력하면 팀장이 검증된 50명 중에서…")가 `max-width:48ch`(104행)에 걸려 2줄로 접히던 것을 `max-width:none`으로 풀어 1200px wrap 폭(콘텐츠 ~1152px)에서 한 줄 표시. 텍스트 실폭 ~820px이라 데스크톱은 한 줄, 모바일은 컨테이너 축소 시 자동 재접힘(white-space:nowrap 안 씀=오버플로 방지). HTML 텍스트 무변경, CSS 1줄만. v52→v53, 루트+빌드 동기.
- 2026-06-28 · v52 · **팀 프롬프트에 팀장 위계 명시**(사용자 요청). 워크플로우 모달의 통합 팀 프롬프트(`buildPrompt()`)에서 팀장의 역할과 "팀장 밑에 팀원들"이라는 구조가 드러나게 보강. ① 도입부: 기존 "혼자서 아래 팀원의 역할을 순서대로 맡아…" 한 줄 → 「이 팀은 팀장인 당신 아래에 팀원 N명(=steps.length)과 검토자 1명으로 구성됩니다. 팀장은 일을 단계로 나눠 맡기고 산출물을 다음 팀원에게 넘기며 검토자 점검으로 품질을 책임집니다.」 + 「당신은 팀장으로서 아래 팀원들의 역할을 순서대로 직접 맡되 매 단계가 누구 일인지 밝히며 진행…」 2문장으로 확장. ② `## 팀 구성과 순서` 명단 맨 위에 "팀장. 오케스트레이터 (당신) — 지휘·핸드오프·최종 책임" 추가, 팀원/검토 줄은 `└ `로 들여 위계 시각화. buildPrompt는 12개 워크플로우 공통이라 전체 적용. v51→v52, 루트+빌드 동기. ※복구 메모: 작업 초 `ls|tail`이 최신을 v48로 오인해 v48 기반으로 v49를 만들어 기존 v49(eyebrow 제거본)와 index.html(루트+빌드, v51 상태)을 덮어씀 → v49.html은 v48+eyebrow 한 줄로 원본 복원, index는 v51 기반 v52로 재작성해 v49·v50·v51 작업(eyebrow·hero-deco·#how 카피) 전량 복구 확인.
- 2026-06-28 · v51 · **히어로 컬러블록 클러스터를 콘텐츠 영역 안으로**(사용자 요청). 기존 `.hero-deco`가 `.hero` 직속·`right:3.5%`(전체폭 기준)라 넓은 화면서 콘텐츠 컬럼(.wrap 1200px) 바깥 여백에 걸림. → 클러스터를 `.wrap` 안으로 이동, `.hero-deco{right:0;top:6px;z-index:-1}`(이미 `.hero .wrap{position:relative;z-index:1}`이라 wrap 기준 절대배치, 컬럼 오른쪽 끝 안쪽; z-index:-1로 텍스트 뒤·크림 배경 위에 표시). v50→v51, 루트+빌드 동기.
- 2026-06-28 · v50 · **① 히어로 장식 알록달록화 + ② #how 카피 윤문**(사용자 요청). ① `.hero::before` 단일 핑크 회전사각형 → 숨김, 대신 `.hero-deco`(히어로 첫 자식 `<div class="hero-deco"><i>×4</i>`) 팔레트 4색(핑크·블루·그린·옐로) 2×2 컬러블록 클러스터(rotate 9deg, nb-b3 보더+nb-sh-sm 그림자). 그라데이션·새 hex 금지 규칙 준수(팔레트색만). 1000px↓ 38px·760px↓ 숨김. ② sec-desc 교체(주술 호응·세련): "…큰 일은 여러 명이 차례로 손을 보탭니다. 빌더가 그 순서를 대신 잡아 줍니다." → "간단한 일은 팀원 한 명이면 끝납니다. 큰 일은 여러 명이 손을 모아야 끝납니다. 오케스트레이터 팀장이 여러분을 대신해 에이전트 팀을 고용합니다."(1·2문장 "일은…끝납니다" 평행, 종결 ㅂ니다 통일). v49→v50, 루트+빌드 동기.
- 2026-06-28 · v49 · **히어로 eyebrow 앞 사각형 마커 제거**(사용자 요청). "AI 에이전트를 고용하라" 앞 `.pulse`(7px 점, 네오브루탈서 검정 사각형) 숨김 → 권위레이어 538행 `.eyebrow .pulse{display:none!important}`(기존 background/animation 규칙 대체, 99·348행 override). HTML `.pulse`는 히어로 1곳뿐. flex gap이라 마커 제거 후 텍스트 앞 여백도 없음. v48→v49, 루트+빌드 동기.
- 2026-06-28 · roasting 팀을 `_workspace`에 파일로 배치 · 사용자: 호출 시 팀원을 파일로 남기게. 기존엔 런타임 일시적(팀원 파일 저장 안 함, 결과물만 출력)이었음. 변경: ③ "팀 구조 제시 · 워크스페이스 배치" 단계 추가 — `_workspace/{YYYYMMDD}_{NN}/`(같은 날 _02·_03 증가) 생성 → `agents/`에 팀원마다 파일(`lead.md` + `{단계}-{slug}.md` 부품 시스템 프롬프트+프런트매터(stage/id/role/io) + `review-{slug}.md`) + `team.md` 빌드시트. ④ 실행은 배치된 `agents/` 파일을 역할 소스로 쓰고 단계 산출물을 `output/{단계}-{slug}.md`에, ⑤ 통과본을 `output/최종결과.md`에 저장. ChatGPT 호환 모드는 파일시스템 없어 인라인(INSTRUCTIONS ③에 명시). README에 "실행 결과(워크스페이스)" 섹션, `.gitignore`에 `_workspace/` 추가(런타임 산출물이라 repo 제외). 동기화: 루트·.claude/skills + github push(248784c→e7ceaea).
- 2026-06-28 · (에셋만) · **카드 하단 ~2px 잔여 띠 제거(2차)**(사용자 요청). v48에서 흰여백은 없앴으나, 헤일로 제거용 2px 침식(binary_erosion)이 **하단 인물 경계까지 깎아 #EDEBEE 그레이 테두리 ~2px**가 남아(어두운 옷 위라 흰색처럼 보임) 선 위에 잔존. 하단은 배경이 없어 침식 불필요 → 처리본 50장 **하단 3px 크롭**으로 인물 옷이 선에 직접 맞닿게 함(루트+빌드). PNG 새 하단행이 인물색(그레이아님) 확인. HTML 무변경(v48 유지).
- 2026-06-28 · (에셋만) · **카드 4쌍 캐릭터 상호 교환(swap)**(사용자 요청). 26↔34, 2↔50, 46↔47, 40↔41 — 각 쌍의 `agent-N.png` 파일 내용을 맞바꿈(루트·빌드·`_orig-colored` 모두). 역할 텍스트는 번호 고정이라 그대로, 얼굴만 교환됨(카드·모달이 번호로 PNG 로드). HTML 무변경. cast.py 매핑도 동기 swap. ※"둘 다 새 인물로 교체"가 아니라 상호 교환으로 해석.
- 2026-06-28 · v48 · **카드 캐릭터 하단 여백 제거(선과 맞닿게)**(사용자 요청). 원인: `.agent-av-box`(이미지 높이=박스 높이) 하단과 별도 `.agent-divider` 사이에 카드 흰배경이 비침(아바타 PNG 하단은 인물, 그레이여백 1~2px뿐이라 PNG 문제 아님). 해결: 구분선을 **박스 하단 보더로 직접** 그림 → `.agent-av-box{...margin:-20px -20px 16px;border-bottom:3px solid #000}` + `.agent-av-box .agent-av{margin:0}`(모바일 margin-bottom:7px 무력화) + `.agent-divider{display:none!important}`. 보더가 박스(=이미지) 하단에 붙어 여백 불가. v47→v48, 루트+빌드 동기. 검증: 미리보기 샌드박스 차단→PIL 시뮬레이션(OLD 흰여백/FIXED 맞닿음) 확인.
- 2026-06-28 · v47 · **lead 사진 위치 정정 + 에이전트 50명 모달에 사진 추가**(사용자 지시 3건). ① 스포트라이트(889행) `lead-color`→**`lead-origin.png`**(네이비 무노트북). ② 팀장 모달(`openLeadModal`,1469행) `lead-origin`→**`lead-color.png`**(노트북). ③ **에이전트 모달 사진 표시**: `openModal`(1434행)에서 `#modalAv`를 `className="mh-av show-av ag"` + `<img assets/agents/agent-{id}.png>`로 채움(기존엔 비워서 숨김). 팀장 모달은 세로비(1086/1449)지만 에이전트 흉상은 정사각형에 가까워, CSS `.modal-info #modalAv.show-av.ag img{aspect-ratio:auto}`(838행)로 원본비율 유지(왜곡·과크롭 방지). 하네스 모달은 `modalAgentInfo` 통째 hidden이라 사진 잔류 없음. v46→v47, 루트+빌드 동기. 검증: 미리보기 서버 샌드박스 차단→코드검토(참조 grep)+PIL 모달 시뮬레이션(사진 정상 배치) 확인.
- 2026-06-28 · (에셋만) · **팀원 50명 zero-base 재캐스팅 + rembg 1px 헤일로 제거**(사용자 요청). 과거 거부/삭제 제약 무시하고 전 후보(폴더00:40·01:50·02:55=145)에서 재선정. 여성 유사문제 해결 위해 변별축(화풍 글로시/플랫/뮤트·헤어 숏밥/뱅/포니테일/브라운/시니어·안경) 분산, 여성 20·남성 30(여성 동질성 커서 20으로 축소, 닮은쌍은 남성화/타화풍 교체). 시니어 안경 트리오는 3화풍으로 분리(5 플랫/14 글로시a36/17 뮤트a5). 31=남성·33=시니어여성·35=포니테일로 분리(기존 31/33/35 여성 닮음 해소), 7(수염)·18(하늘색 글로시)로 분리. **헤일로 fix**: rembg 알파를 `binary_erosion(2px)`+`GaussianBlur(0.6)` 후 그레이#EDEBEE 합성 → 실루엣 흰테 제거(좌측엣지 확대 OLD/CLEAN 비교 확인). 기존 agent-1~50 삭제 후 재생성, `_orig-colored` 슬롯백업 갱신, 루트+빌드 동기. HTML 무변경(v46 유지). 매핑·검수그리드는 세션 scratchpad(cast.py). 남은 청년남성 동질성은 풀 특성상 일부 잔존.
- 2026-06-28 · v46 · **① 스포트라이트 팀장 노트북 사진 복원 + ② 카드 아바타 박스 full-bleed 그레이**(사용자 요청). ① 스포트라이트(887행) `lead-origin.png`→`lead-color.png`(노트북 든 사진+그레이#EDEBEE배경). 모달(1467행)은 "이 페이지에서는" 범위라 `lead-origin.png` 유지(스포트라이트=노트북, 모달=네이비 무노트북). ② 카드 흰여백 원인=`.agent-av`가 width:auto(≈155px) 가운데정렬+카드 bg #fff. 해결: 렌더에서 `<img class="agent-av">`를 `.agent-av-box`로 감쌈 + CSS `.agent-av-box{background:#EDEBEE;margin:-20px -20px 0;display:flex;justify-content:center;align-items:flex-end;overflow:hidden}`(카드 padding20 상쇄해 좌우·상단 full-bleed, 이미지 중앙→박스 전체 그레이). v45→v46, 루트+빌드 동기. ※검증: 미리보기 서버가 프로젝트 폴더 접근차단(Operation not permitted)으로 브라우저 렌더 불가→코드검토+PIL 카드 시뮬레이션(before 흰여백/after 전체그레이)으로 확인. 임시 서버파일·launch.json 정리.
- 2026-06-28 · (에셋만) · **팀원 50명 아바타 배경 흰색→그레이(#EDEBEE)**(사용자 요청, 팀장 영역은 그대로). 흰 치환은 셔츠 등 인물 내부 흰색까지 바뀌므로, `_orig-colored/agent-1~50.png`(슬롯 컬러 원본)에서 rembg(u2net_human_seg) 재분리 후 **그레이 #EDEBEE에 합성** → 루트+빌드 동기. 그레이값은 lead-origin.png 배경(#ECE9EC)·CSS `.lead-spot-av` 토큰과 일치. lead.png/lead-origin.png/lead-color.png 미변경. HTML 무변경(아바타 PNG에 배경색 베이크). 현재 선택분(2차 재선정)은 크랍 없음→손실 없음.
- 2026-06-28 · v45 · **팀장 사진을 `lead-origin.png`로 교체**(사용자 요청). 사용자가 루트 `assets/agents/lead-origin.png`(안경·네이비셔츠·연회색배경, 노트북 없는 다른 포즈, 1085×1450) 추가. 스포트라이트(885행 `lead.png`→`lead-origin.png`)와 팀장 모달(1465행 `lead-color.png`→`lead-origin.png`) 두 곳 모두 교체(사진 일관성). 치수 사실상 동일(모달 `aspect-ratio:1086/1449` 그대로 OK). `lead-origin.png`를 빌드 `output/.../assets/agents/`에도 복사. v44→v45 후 루트+빌드 index.html 동기. 구 `lead.png`/`lead-color.png` 기능 참조 0개. (정적서버 샌드박스 권한오류로 브라우저 렌더검증 불가 → 파일레벨 검증: 양쪽 index.html 참조 2개씩·이미지 양쪽 존재·치수 호환.)
- 2026-06-28 · (에셋만) · **팀원 50명 사진 전면 재선정(2차)**(사용자 요청 "지우고 다시"). 기존 agent-1~50 삭제 후 세 풀에서 재선정 → 흰배경 처리 → 루트+빌드 동기. 거부 얼굴 제외(01/agent-1·2·4·5·12·28·31·38·47, 00/agent-2·42·43). 화풍 혼합: 폴더 00:13 / 01:11 / 02:26, 성별 25F/25M, 시니어 6명(남5+여1 av01). ※주의: 소스 폴더 `agents50-00`이 50→40장으로 줄어 **agent-41~50 유실**(folder01·02는 온전, 원인 미상·아마 사용자 수동 삭제). 새 매핑은 유실분 미사용. 컬러 원본 백업 `assets/agents/_orig-colored/`(슬롯번호 기준) 갱신. lead.png는 유지. 흰배경은 직전 확립 선호라 신규분도 동일 적용(원본 배경 원하면 재처리 가능). 선정시트는 세션 scratchpad.
- 2026-06-28 · v44 · **팀장 모달 좌측에 컬러 원본 사진 추가**. 모달 아바타칸 `.mh-av`는 전역 `display:none!important`(641·677행)이라, 팀장 모달에서만 `#modalAv`에 `show-av` 클래스를 붙여 되살림(`openLeadModal`). 에이전트(50명) 모달은 `openModal`에서 `show-av` 제거→여전히 숨김. 이미지는 흰배경화 이전 **컬러 원본**(`assets/agents/_orig-colored/lead.png`)을 `assets/agents/lead-color.png`로 루트+빌드 복사해 사용("원본으로" 요청). CSS `#modalAv.show-av img`: height 210(모바일 170)·`aspect-ratio:1086/1449`(로드 전 폭 확정→접힘 방지)·3px 검정 보더·5px 그림자. 검증(eval): 팀장 모달 아바타 표시(127×170, src=lead-color.png, 보더 3px), 에이전트 모달 아바타 display:none·show-av 없음 확인.
- 2026-06-28 · (에셋만) · **아바타 9명 교체 + 엣지 크랍**(사용자 요청). 교체(전부 흰배경 처리): 1→02/avatar-16(F), 4→02/avatar-35(F), 8→00/agent-9(M), 24→02/avatar-22(F·여성지정), 25→00/agent-2(F), 31→00/agent-23(M), 32→02/avatar-50(M), 34→00/agent-41(M시니어), 36→00/agent-30(F). ※교체 9건 중 8건이 폴더01(광택 화풍)이라 해당 화풍 비선호로 판단 → 교체분은 폴더00/02 우선 선정. 크랍(흰배경 후 하단 잔선 정리): 3=좌1·하1, 44/47=하3, [6·8·10·12·16·17·19·20·24·27·28·37·39·40·42·45·49]=하2. 루트+빌드 동기, 50장 유일성·성별균형(24F/26M) 유지.
- 2026-06-28 · v43 (prompts.js) · **팀장(`lead`) 프롬프트 RED 채점·개선**. Before 9.0(불합격): 정확성 8.5(롤스터 지식 가정→이름 허위 위험)·완결성 8.5(설계/실행 운영모드 미분리). 개선: `## 팀원을 고르는 법`(목록 있으면 그 안에서만, 없으면 이름 지어내지 말고 역할명으로)·`## 운영 모드`(설계/실행 분리)·검토 루프 명문화·다음액션 환경독립화·원칙에 허위생성 가드. After 9.58(합격, 2390자). HTML 무변경→v43.html=v42 사본(prompts.js-only 마일스톤). 루트+빌드 prompts.js 동기화. 평가기록 `_workspace/2026-06-28_팀장-프롬프트-루브릭-평가.md`. (skill-creator 슬래시로 진입했으나 실제 요청은 단일 프롬프트 RED 평가라 프로젝트 정본 루프로 처리, 스킬 생성 하네스 미사용.)
- 2026-06-28 · (에셋만) · **모든 아바타 배경 흰색화**(사용자 요청). `agent-1~50.png` + `lead.png`(총 51장)을 `rembg`(u2net_human_seg)로 인물 분리 후 흰 배경에 합성. 루트(`assets/agents/`)+빌드(`output/agent-team-builder/assets/agents/`) 동기. 컬러 배경 원본은 `assets/agents/_orig-colored/`에 백업. ※ 후보 풀(`agents50-00/01/02`)은 미사용 소스라 그대로 둠(필요 시 동일 방식 적용 가능). rembg/onnxruntime는 `--user`로 설치(python3.9), u2net_human_seg 모델 `~/.u2net/`.
- 2026-06-28 · (에셋만) · **agent-50 교체**(사용자 요청). 50번 리포트 자동화 담당: `00/agent-43`(폴더00 안경 남성) → `02/avatar-09`(폴더02 플랫, 안경 남성·블루셔츠). 루트+빌드 동기, 50장 유일성 재확인.
- 2026-06-28 · v42 · 팀장(01) 섹션 헤딩 문구 확정. "목표를 팀으로 바꾸는 한 사람"(v40) → "목표를 수행하는 팀을 설계하는 리드"(v41) → **"목표를 이루기 위한 팀을 설계하는 오케스트레이터"**(v42). 주술 호응 교정: 목표+수행(약한 연어)→목표+이루다(팀이 목표를 이룸, 오케스트레이터가 팀을 설계함).
- 2026-06-28 · (에셋만) · **팀원 50명 사진 전면 재선정·교체**. 후보 풀 `assets/agents/agents50-00`(agent-1~50, 채도낮은 플랫 화풍)·`agents50-01`(46장, 고해상도 정밀 렌더)·`agents50-02`(avatar-01~55, 밝은 플랫)에서 역할 적합성·얼굴 고유성·화풍 분산 기준으로 50장 선별 → `assets/agents/agent-1.png~agent-50.png`(루트) + `output/agent-team-builder/assets/agents/`(빌드)에 동기 복사. 기존 빌드 세트는 `output/agent-team-builder/assets/agents/_prev-selection/`에 백업. 분포: 폴더 00:12 / 01:18 / 02:20장, 성별 ≈ 24F/26M, 시니어 5명(문헌정리·IR보고·전략기획·문서검토·비판검토)에 배치, 안경=분석/검토 역할. 위험군(폴더00 안경 남성 5·시니어 5) 확대비교로 중복 없음 확인. ※ 세 폴더가 사실상 3개 화풍뿐이라 "50개 전부 다른 화풍"은 물리적으로 불가 → 3풀 혼합으로 다양성 극대화.
- 2026-06-28 · v40 · **섹션·메뉴 순서 재배치**(사용자 제안). 기존 01 팀 구성→02 팀장→03 팀원에서 → **01 팀장(#lead) → 02 팀 구성(#how, dark) → 03 팀원 카탈로그(#catalog)**. 팀장 섹션을 how 섹션 앞으로 이동(히어로가 "팀장이 팀을 짜준다" 약속 → 바로 팀장 등장). kicker 번호 lead 01·how 02로 변경(catalog 03 유지). 헤더·푸터 내비도 **팀장 · 팀 구성 · 팀원 50명** 순으로(스크롤 순서와 일치). 검증(eval): 섹션 01/02/03 순서·라벨, 헤더·푸터 내비 순서, `id="lead"` 1개(중복 없음), 그리드 50카드 확인.
- 2026-06-28 · v39 · 팀장 섹션 후속 3종. ① **내비에 「팀장」 추가**: 헤더(`#navLinks`)·푸터(`.foot-col`) 모두 `팀 구성(#how) · 팀장(#lead) · 팀원 50명(#catalog)` 3개로(섹션 구조와 일치). ② **팀장 사진 크롭 변경**: `.lead-spot-av img` `object-position:center 18%` → **`center top`**(머리 위 여백은 유지, 하단을 잘라 보여줌). ③ **헤딩 능동형으로**: "목표를 받아 팀을 짜는 한 사람"(수동) → **"직접 팀을 짜고, 끝까지 끌고 가는 사람"**. 검증(eval): 헤더·푸터 내비 3링크 확인, objectPosition `50% 0%`, 헤딩 텍스트, `#lead` 앵커 존재 확인.
- 2026-06-28 · v38 · 팀장을 **독립 섹션으로 격상 + 섹션 재번호**. 구조: **01 팀 구성(how) → 02 팀장(신규 `<section id="lead">`) → 03 팀원 카탈로그(기존 catalog, kicker 02→03)**. 팀장 스포트라이트(`#leadSpot`)를 카탈로그 상단에서 빼 02 섹션 본문으로 이동. 3가지 수정: ① **연한 배경**: 블록 bg `var(--pink)`#FE90E8 → `var(--blue)`#C0F7FE. ② **사진 안 보이던 버그 수정**: `.lead-spot-av`를 `flex:0 0 auto;width:172px`(접혀서 0폭 됨) → **`flex:0 0 210px`(고정 basis) + `position:relative`**, 이미지를 `position:absolute;inset:0;width/height:100%;object-fit:cover;object-position:center 18%`로 칸을 꽉 채움(접힘 불가). ③ **카피 윤문**(주술 호응·프로): 섹션 desc·블록 desc 재작성. 블록 desc="50명의 팀원은 저마다 하나의 역할을 맡습니다. 팀장은 그 위에서 목표를 결과물 단위로 나누고, 필요한 팀원만 골라 일의 순서와 인계 지점을 정합니다. 마지막에는 검토자를 배치해, 검토를 통과한 결과물이 나올 때까지 팀을 이끕니다." 검증(뷰포트 1280 강제): 섹션 01/02/03 순서·라벨 확인, 블록 bg rgb(192,247,254), 아바타칸 210px·이미지 206×169 채움(접힘 해결), 그리드 50카드·팀장 모달(1929자) 정상. (헤드리스 스크린샷은 빈 화면 계속—캡처 한계, computed/박스값으로 검증.)
- 2026-06-28 · v37 · 팀장 스포트라이트 아바타 칸(`.lead-spot-av`) 배경을 `var(--cream)` → **`#EDEBEE`**(=`lead.png` 자체 배경색, 네 모서리 모두 rgb 237,235,238)로 변경. 사진이 칸보다 좁아(height 188 기준 약 141px) 양옆에 크림색 띠가 보이던 문제 해결 — 이제 사진 배경과 칸 배경이 일치해 이음매 없음. (사용자 지시 "배경은 사진과 같게". 디자인 토큰 새 hex 금지 예외: 장식색이 아니라 사진 정합용.) preview_inspect로 `rgb(237,235,238)` 적용 확인(이 헤드리스 프리뷰는 뷰포트 폭 0이라 스크린샷·width 측정 불가, computed 색으로 검증).
- 2026-06-28 · v36 · 카탈로그(50명 그리드) **바로 위에 「팀장 · 오케스트레이터」 스포트라이트 블록**(`#leadSpot`) 추가. `assets/agents/lead.png`(반신·노트북 든 권위 구도) + 역할 라벨 + 특성 1문단 + "시스템 프롬프트 보기 →". 클릭/Enter → `openLeadModal()`이 기존 모달 재사용해 팀장 프롬프트 표시(복사·.md 다운로드 포함). 팀장 프롬프트는 `assets/prompts.js`에 **`lead` 키**(숫자 1~50과 충돌 없음, 1929자, 공통 5차원 품질기준 동일 양식)로 추가. `openModal()` 첫 줄에 `#modalAv` 비우기 추가(팀장 본 뒤 에이전트 모달에 lead 이미지 잔상 방지). CSS는 `</head>` 직전 새 `<style>`(`.lead-spot*`, 컬러블록 스킨, 640px↓ 세로 스택). **원칙 확정: 50=부품(흉상 그리드), lead=라우터/오케스트레이터(별도 블록). 둘을 같은 그리드에 섞지 않음** → lead는 51번째 카탈로그 카드로 넣지 않는다. 검증: preview_inspect로 핑크#FE90E8·4px보더·8px그림자·flex 확인, 모달 1929자 프롬프트·복사/다운로드 버튼·아바타 로드(1086×1449) 확인. (dev 프리뷰용 `.claude/launch.json`+`.claude/static-server.js` node 정적서버 추가.)

## 현재 상태 (스냅샷)
- 배포본: `index.html`(루트) = `output/agent-team-builder/v34.html`. **v34 = v31/v33 HTML + 팀 빌더(라우터)**. 히어로 아래 `#picker`가 「팀 빌더」로 격상(목적 입력→구조형 팀 설계→복사). 하네스 데이터는 `assets/router.js`(전역 `HARNESS`/`HARNESS_CATS`/`MODIFIERS`, prompts.js 다음 동기 로드). 정본은 `PLAN.md`. (v32~33은 `assets/prompts.js`만 바꾼 마일스톤, HTML은 v31과 동일했음.) **v31 = v30에서 50개 시스템 프롬프트(`var PROMPTS`, 148KB)를 외부 `assets/prompts.js`로 분리.** index.html 236KB→88KB(63% 감량). `<script src="assets/prompts.js"></script>`를 메인 `<script>` 바로 앞에 두어 IIFE 실행 전 전역 PROMPTS 정의(file://·http 모두 작동, fetch 아님). prompts.js는 루트·빌드 `assets/` 두 곳에 동일 사본. **메인 디자인은 v25.html(네오브루탈리즘) 베이스**. v30 = v29(v25 디자인 + 마크다운 프롬프트 + 다운로드 버튼 id 충돌 수정) + 히어로 `.btn-ghost`(팀원 만나보기) 플랫화. (사용자가 "v24 디자인으로" 지시했다가 v24=옛 람보르기니 라이트임을 확인하고 "하지 마"로 철회, 네오 유지 확정.)
- 프롬프트: 50개 전부 **harness-100 수준 + Opus 4.8 가이드 + 마크다운 형식**. 구조: `# 직책(영문)` 제목 → 역할+경계+미션 문단 → `## 핵심 역할`(번호) → `## 도구`(언제·어떻게) → `## 워크플로우`(받음/전달/결손 + "답하기 전에 차근차근 생각" 사고 순서) → `## 출력 형식` → `## 예시`(few-shot) → `## 품질 기준 (10점 만점)`(v33부터 **공통 5차원 0~10 채점표**: 정확성·근거/목적·완결성/구조·형식/실행력/언어·톤, 50개 전원 동일, 총점=5평균, 9.5 미만 자가수정, 산출물 끝에 `자기평가:` 한 줄 출력. v32에서 역할 고유 6번째 차원을 넣었다가 v33에서 5차원으로 통일) → `## 원칙`(불변 규칙 + 자연스러운 한국어). 한자 0(v32에서 上中下比 제거). Opus 4.8 반영: 사고 기본 off 대비 "답하기 전에 생각" 명시, 리터럴·범위 명시, 긍정 지시 우선. em dash 0, 평균 1316자. `var PROMPTS` 객체(1~50). 생성=scratchpad `gen10.py`(D) + `gen_pro.py`(E) + `gen_md.py`(마크다운 조립, 둘 다 import).
- 디자인: **컬러 블록(네오브루탈리즘)** · v25부터. 파스텔 네온 5색(#FE90E8/#C0F7FE/#99E885/#F7CB46/#FFDC8B)+흑/백/오프화이트(#FFFDF5), 4px 검정 보더 + 8px 오프셋 검정 그림자, **직각(radius 0)**, Pretendard 800~900 디스플레이 + Space Grotesk 모노 라벨, 본문 1200px. 권위 스킨은 `</head>` 직전 마지막 `<style>` 블록. (v24까지는 람보르기니 라이트=흰 배경·골드·둥근 모서리였음.)
- 콘텐츠: 범용 50 에이전트 팀원(8섹션) + 워크플로우 5종. 라우터 = "목표 → 부품 조합".
- 인터랙션: 마우스다운 리플 + 마우스오버 물결 + 로고 360° 회전. (픽셀 캐릭터 호버 애니메이션은 네오브루탈리즘 전환 때 제거됨.)
- 모달: **좌우 2단**(`.modal-grid`). 좌 `.modal-info`=역할·설명(No.·섹션·직책·영문·설명·메타칩, 아바타칸은 `display:none`), 우 `.modal-main`=시스템 프롬프트 박스 + **복사/.md 다운로드 2버튼**. `downloadMd(id)`가 Blob로 `agent-NN-slug.md` 생성. 720px↓ 1단.
- 캐릭터(레거시·미사용): `pixelAvatar(a,lg)` 함수는 코드에 남아 있으나 현 디자인에서 호출하지 않음(그리드·모달 모두 컬러 블록). 아래는 옛 픽셀 아바타 디자인 기록. `pixelAvatar(a,lg)` 생성기 v3. **32그리드 + 자동 외곽선 + 림라이트/폼섀도 반투명 오버레이**. **동양인**(따뜻한 밝은 피부4·흑발/진갈색6), **남녀 혼합**(id 짝수=여성: 롱·단발·포니테일·번 + 속눈썹·입술 / 홀수=남성: 숏·가르마·슬릭·버즈 + 수염 일부). 섹션 팔레트 8종(SEC2)으로 카테고리별 옷색 구분. 카드 72px·모달 116px. 시드는 parity 붕괴 피하려 `(id*7%13)%4`(피부)·`((id/2)|0)%4`(헤어) 등 사용.

## 변경 로그 (최신이 위)
- 2026-06-28 · v35 · **아바타 해상도 복구(무손실) + 하단 선 flush**(사용자: 원본 해상도 깨짐, 이미지 밑 여백 없이 선에 붙어야). 원인1=직전 합성이 ~150px 원본 인물을 280×300으로 **업스케일**→흐려짐. 해결1: 캔버스 **196×188**(최대 인물 156×181보다 큼)에 인물을 **원본 스케일 그대로 paste(다운스케일 0건)**→무손실. 가로중앙·하단정렬(bottom flush), bg=셀 좌상단 median. 원인2=`.agent`가 `display:flex;gap:6px`라 이미지·divider 사이 6px 흰틈. 해결2 [v35 line 597]: `.agent-divider` margin `0`→`-6px -20px 16px`(flex gap 상쇄해 선이 이미지 하단에 바로 맞닿음). 표시는 `height:170px`(196×188→177×170 약한 다운스케일=선명). 검증: 실카드 시뮬(1·8·16·32) 선명·여백 작음·하단선 즉시 맞닿음 확인. 50개 196×188 두 assets·두 index.html 동기화.
- 2026-06-28 · v35 · **아바타 확대 축소(원래 크기로) + 인물 여백 합성**(사용자: 너무 확대됨, 더 줄여 원래 사이즈로). 원인: 풀폭(height:auto, 286×352)이라 카드에서 인물이 큼. 해결: ①크롭 재생성 — 인물 bbox(밝기<228·여백골 thr0.06 박스 내)를 **흰 배경 캔버스(280×300)에 합성**, 인물 높이=프레임 74%(FRAC0.74)로 축소·가로중앙·하단정렬(여백0), 캔버스 bg=셀 좌상단 8×8 median(인물 자체 회백 배경과 매칭해 박스 이음새 없음). 좌우·상단 여백 생겨 덜 확대돼 보임. ②[v35 line 595] `.agent-av`를 풀폭(`width:calc(100%+40px);height:auto`)→**컴팩트 중앙(`width:auto;height:170px;object-fit:cover;align-self:center;margin:0 auto`)**. divider는 flush 유지. 검증: 카드목업(1·8·16·32) 인물 작게·여백·하단선 맞닿음 확인. 50개 전부 280×300 두 assets 동기화, 두 index.html 동기화. (추가 축소 원하면 height:170 더 낮추거나 FRAC↓.)
- 2026-06-28 · roasting 메인=Claude 포지셔닝 · 사용자 "메인을 클로드로". ChatGPT 추가로 "Claude Code + ChatGPT" 동급처럼 보이던 걸 정리: 배지 `Native: Claude Code`(주, 보라) + `Also on: ChatGPT`(보조, 회색), 인트로에 "기본(네이티브) 플랫폼은 Claude Code(독립 에이전트·진짜 독립 게이트로 9.5 최강 작동), ChatGPT는 호환 모드" 명시, ChatGPT 섹션 제목 "(호환 모드)" 강등. repo description은 이미 "Claude Code 스킬"이라 유지. github push(d5f5e2d→248784c), 루트·.claude/skills 동기화.
- 2026-06-28 · v35 · **아바타 50개 정밀 재크롭(인물 중앙·하단 여백0·동일 크기)**(사용자: 정확히 보고 사람 가운데, 하단 여백 없이 모두 동일 크기). 인물별 검출 후 합성: 밝기<228 마스크→여백 골(thr0.06)로 열10·행5 박스→박스 내 인물 bbox(행/열 content>0.12) + **머리 중심**(상단45% 영역 content>0.15의 좌우 중점)으로 가로 중앙 기준. 출력 **전원 260×320 동일**: 인물 높이가 프레임의 97%(FRAC0.97) 차지하도록 스케일, **하단=인물 bbox 바닥(fy1+3)에 맞춰 여백0**, 가로=머리중심, 크롭폭이 이웃안전폭 초과 시 축소(이웃 잔상 방지), 세로 top<0이면 보정. LANCZOS 리사이즈. 검출 통계: 인물 H 140~173·W 95~152. CSS는 직전 `width:calc(100% + 40px);height:auto`(풀폭 자연비율) 유지 — 이미지 픽셀만 교체. 검증: 50그리드(하단선 표시)로 전원 중앙·바닥맞닿음·균일 + 카드목업(1·8·16·32) 머리~어깨·상의 보이고 검정선 맞닿음 육안 확인. 두 assets 폴더 동기화(전부 260×320).
- 2026-06-28 · v35 · **카드 아바타 자연비율 풀폭으로 수정(상의 잘림 해결)**(사용자: 얼굴만 커지고 상의 안 보임). 직전 `height:190px;object-fit:cover;object-position:top` 배너가 카드폭(~286)에서 확대돼 얼굴만 크고 상의 잘림. [v35 line 595] `.agent-av`를 `width:calc(100% + 40px);height:auto;margin:-20px -20px 0`(자연비율 풀블리드, 머리+어깨+상의 전부 표시, 하단 구분선 맞닿음)로 변경. 검증: 실제 카드폭 286 시뮬 목업(1·8·16) 상의 보이고 하단 선 맞닿음 확인. 두 index.html 동기화.
- 2026-06-28 · roasting 크로스플랫폼(ChatGPT 지원 추가) · 사용자: "Claude뿐 아니라 ChatGPT에서도". ChatGPT엔 서브에이전트/TeamCreate 없음 → 스킬의 3티어 폴백(단일 모델 순차)이 그대로 ChatGPT 실행 모드, 게이트는 냉정 재독 독립 검토. references 온디맨드 대신 Custom GPT Knowledge 업로드. 신설 `roasting/platforms/chatgpt/`: `INSTRUCTIONS.md`(Custom GPT 지침 본문, SKILL 폴백 경로 응축·순차 실행·지식파일 참조·브라우징) + `SETUP.md`(5분 셋업: 지침 붙여넣기+references 4개 Knowledge 업로드+Web Search 켜기+사용법+Claude와 차이표). README에 ChatGPT 섹션·`Runs on: Claude Code + ChatGPT` 배지·구조 트리 갱신. 동기화: 루트·.claude/skills + github push(9f5a0df→d5f5e2d).
- 2026-06-28 · roasting 스킬 9.4→9.5 폴리시(독립 적대적 리뷰 기반) · /skill-creator로 "10점 평가+개선". 독립 리뷰어 서브에이전트가 현 스킬 채점(7.8/10)→6개 결함 지적. 6개 다 반영(SKILL.md/README/execution-modes 문서만, prompts.js 불변): ①부품 `자기평가`(핸드오프 전 내부 바닥선, 비출력) vs ⑤ 독립 게이트(외부 합격선, self-pass도 탈락 가능) **2층 명문화**(자기채점 모순 해소) ②README "두 번 탈락"→본문 "보강 1회" 규칙과 일치 ③단일 산출물이면 작성1+게이트1 **2스텝 하한 예외**(LEAD+≥3 강제 해제) ④형제 스킬 영역(포맷변환·단순조회·단발검토)은 양보 ⑤폴백 게이트 독립성 약화 **한계 명시 + cold re-read** ⑥다단계 보강 에이전트=주결함 단계 역할 프롬프트 착용 + 대용량 입력 digest 처리. 추가로 스킬 자체 원칙("긴 줄표 피한다") 위반인 em dash 전부 제거(산문→마침표, 제목·구분→·/:, 다이어그램→:, harnesses/catalog 생성물까지; agent-prompts 0). 재검토: 6/6 CLOSED, 종합 9.4→**9.5**. 동기화: 루트·.claude/skills + github push(6b8470b→9f5a0df).
- 2026-06-28 · v35 · **아바타 이웃 잔상 제거 + 풀블리드 배너 레이아웃(하단 선 맞닿음)**(사용자: 8·16번 오른쪽 옆사람 잔상, 1번 왼쪽 어색, 하단 흰 여백 없애고 선에 붙이기). 원인1=여백 골 *중심* 컷이 이웃 어깨/머리를 일부 포함(특히 어깨 높이대). 해결1: 경계를 골 *안쪽 가장자리*(인물이 실제 끝나는 지점, thr 0.06)로 변경 — 열 [(18,170)(195,339)(360,513)(537,683)(712,861)(888,1040)(1065,1214)(1244,1396)(1421,1571)(1596,1748)], 행 [(19,186)(205,368)(385,548)(560,715)(724,883)], 추가 INSET 3px. 짧은변 340px LANCZOS. 원인2=144 정사각 가운데 배치라 인물 옆·아래 흰 배경. 해결2 [v35 line 595·597]: `.agent-av`를 `height:190px;object-fit:cover;object-position:center top;align-self:stretch;margin:-20px -20px 0`(카드 패딩 상쇄 풀블리드 배너, 머리 상단 정렬로 안 잘림), `.agent-divider`를 `border-top:3px solid #000;margin:0 -20px 16px`(이미지 하단에 여백 0으로 맞닿음). 검증: object-fit cover 시뮬 카드목업(1·8·16) + 배너 50그리드 — 머리 안잘림·어깨가 선까지 채움·이웃 잔상 0·하단 흰여백 0 육안 확인. 두 index.html·두 assets 동기화.
- 2026-06-28 · roasting 스킬을 github.com/airoasting/roasting에 배포(기존 통째 교체) · 사용자: "이 repo에 완전히 새롭게 올리고 기존건 지워". **주의 후 진행**: 기존 repo는 빈 게 아니라 5-Color Harness Roasting 엔진(156파일: skills 94·tests 31·scripts·plugin, CLAUDE.md 1순위 대표작)이었음 → 멈추고 "정말 덮을지/새 repo/추가" 물음 → 사용자 "통째로 덮어쓰기" 재확인. 안전장치로 **강제 푸시 안 함**(히스토리 보존): clone(--depth1)→기존 파일 전부 rm→roasting/ 내용 루트에 복사(+.gitignore)→commit→`git push origin main`(d0f5b8d→6b8470b fast-forward). 결과 main=8파일(SKILL/README/LICENSE/.gitignore/references×4), 삭제153·추가5. **기존 엔진은 직전 커밋 d0f5b8d에 보존**(git checkout d0f5b8d -- . 또는 revert 6b8470b로 복구). repo description도 에이전트 팀 빌더로 갱신. gh 인증=airoasting(소유). 네이밍 충돌 주의 남김: repo "roasting"이 이제 5-Color 엔진이 아니라 에이전트 팀 빌더를 호스팅.
- 2026-06-28 · v35 · **아바타 크롭 보정(옆 사람 어깨 물림 제거)**(사용자: 오른쪽 인원 어깨가 들어옴). 원인: 단순 1/10·1/5 균등 분할은 인물 사이 여백을 무시해 이웃 어깨가 셀에 걸림. 해결: 원본을 흑백 밝기(<225=인물)로 변환→열/행별 content 밀도에서 **여백 골짜기(밀도<0.05) 검출**→골 중심을 경계선으로 사용. 검출된 경계: 열 [9,183,350,525,697,874,1053,1227,1408,1583,1761](10열), 행 [9,195,376,553,719,885](5행). 각 박스를 짧은 변 320px 기준 LANCZOS 업스케일 저장(카드 `object-fit:cover`가 정사각 중앙 크롭). 파일명·매핑·HTML 변경 없음(이미지 픽셀만 교체, 두 assets 폴더 동기화). 검증: object-fit cover 시뮬레이션 50그리드로 전원 중앙 정렬·이웃 어깨 0 육안 확인.
- 2026-06-28 · v35 · **카드 아바타를 섹션 8종 공유 → 에이전트별 50개 고유 캐릭터로 교체**(사용자: 50agents.png를 50개로 쪼개 성격 맞춰 매핑). 원본 `assets/agents/50agents.png`(1774×887, 10열×5행 수채화 인물). PIL로 셀 177×177 크롭→256×256(LANCZOS) 저장, `assets/agents/agent-{1..50}.png` + 빌드 폴더 사본. 렌더 [v35 line 1377] `src`를 `char-(a[3]+1).png`(섹션 인덱스) → `agent-{a[0]}.png`(에이전트 id)로 변경. 매핑=그리드 위치 N→에이전트 N 기본, 성격 더 맞는 4쌍만 교체: (3마켓↔7출처검증=캐주얼/안경), (9트렌드↔16비용=젊음/시니어), (17IR↔22요약=시니어신뢰/담백), (21이메일↔38브레인스토밍=단정/에너지). 판단 기준: 안경=분석형, 미소=대인형, 시니어=권위형, 미니멀=효율형. 검증: scratchpad 라벨 시트로 50개 얼굴 1:1 육안 확인(★4쌍 교체 반영 확인), bijection assert 통과, grep char- 0건. 기존 char-1~8.png는 미사용으로 잔존. (정적 file://라 preview 서버는 샌드박스 막힘 — 시트 증빙으로 대체.)
- 2026-06-28 · roasting README 상단 배지 추가 · 사용자가 harness 스타일 shields 배지 row 이미지 제시. harness 고유값(8k stars·6 Architectures·L3 Meta-Factory·EN|KO|JA)은 우리 사실과 달라 **roasting 진짜 값으로 적응**(지어내지 않음): Version 1.0.0 / License Apache 2.0 / Claude Code Skill / Agents 50 Verified / Recipes 12 Harnesses / Mode Agent Teams / Layer L3 Execution / Sub-layer Verified Team Executor / Quality 9.5 Gate / README KO. H1 직후 2줄 shields.io 정적 배지(10개). curl로 200 image/svg+xml 렌더 확인(`Sub--layer` 이스케이프 정상). 루트·.claude/skills 동기화.
- 2026-06-28 · roasting README + Apache 2.0 LICENSE · `roasting/README.md`(한국어: 무엇/설치/사용법 `/roasting`/티어드 실행 모드/9.5 게이트/장착 도구 5종 표/구조/영감(revfactory/harness 영감, 내용은 독립 작성)/라이선스) + `roasting/LICENSE`(Apache License 2.0 전문 + Copyright 2026 AI ROASTING (Jayden Kang)). 루트·`.claude/skills/roasting/` 동기화, `.DS_Store` 제거.
- 2026-06-28 · 에이전트 도구 5종 장착 · 사용자가 AI ROASTING 자사 웹도구 5개 URL을 주고 "필요한 팀원에게 쥐어줘". 각 도구 정체 확인 후 `assets/prompts.js`의 해당 에이전트 `## 도구` 섹션에 "전용 도구. <언제·어떻게>(URL)" 한 줄씩 추가(15명). 매핑: **strategy**(70 전략 프레임워크 갤러리, airoasting-strategy)→34·13·39·12 / **5color**(5인 페르소나 검토 지침 생성, 5color.vercel)→47·49·46·25 / **slide**(35 HTML 슬라이드 템플릿, airoasting-slide)→24·23 / **blog**(글로벌 리서치 인사이트, airoasting-blog)→1·9·3 / **skill**(엄선 실무 AI 스킬, airoasting-skill)→45·44. 구현: scratchpad `add_tools.js`가 PROMPTS 로드→대상 id의 도구 섹션에 삽입→`var PROMPTS={...}` 재출력(numeric 키 유지, JSON.stringify로 이스케이프). 도구 섹션 문구가 3종이라(일반/웹검색형 1·3·9/계산형 39) 2패스로 처리. 재생성: `build_refs.js`로 skill `references/agent-prompts.md` 갱신. 동기화: prompts.js 빌드→루트 assets, roasting→.claude/skills. 검증: node 재파싱 50키·15/15 매핑(strategy4·5color4·slide2·blog3·skill2), preview 웹앱 PROMPTS 로드·모달 도구줄 렌더 확인. (웹앱 모달·스킬 실행 양쪽에 자동 반영.)
- 2026-06-28 · roasting 스킬 업그레이드 (실제 에이전트 팀 실행) · revfactory/harness(메타-스킬, 팀·에이전트·스킬 생성기)와 비교 후, 우리 강점(검증·게이트·즉시실행)은 두고 harness의 "진짜 멀티에이전트 실행"만 차용. 단일 LLM 순차 역할극 → **티어드 실행**: ①에이전트 팀(TeamCreate/SendMessage/TaskCreate) ②서브에이전트(Agent, 파이프라인+팬아웃) ③순차 역할극(폴백). 핵심 가치=독립성: 팀원이 별도 컨텍스트라 **검토자가 진짜 독립**(자기채점 아님)+독립 단계 병렬. `references/execution-modes.md` 신설(모드 비교·병렬/순차 판단·팀원/독립검토자 프롬프트 템플릿·보강 루프·입력 전달 주의). SKILL.md ④/⑤ 재작성(124줄). 검증: 서브에이전트로 실제 end-to-end 실행(원격근무 정책)—팀원 3명 별도 agentId로 스폰·파이프라인 핸드오프·검토자 별도 스폰이 **2회 불합격(6.5→9.2)** = 게이트 진짜 작동 입증. 테스터 갭 3개 패치: 보강 1회 후 미달 시 탈출 규칙(형식이면 팀장 마감/실질이면 한계 명시·가짜합격 금지), 다단계 결함 묶어 보강 허용, 서브에이전트 입력은 전문이 곧 대상이면 통째로. 루트·`.claude/skills/roasting/` 동기화.
- 2026-06-28 · roasting 스킬 신설 · **웹 팀 빌더를 Claude Code 스킬로**(사용자: "/roasting, 목적 입력→50명 기반 팀 짜주는 스킬, 10점"). PROJECT.md 3층 스택 [3](실행 엔진) 시드. 동작=설계+**실행**(사용자 결정): 목적→레시피 매칭/카탈로그 조합으로 팀 설계→팀장이 각 역할 순차 수행(handoff)→9.5 적대적 게이트→최종본. 구조: `roasting/SKILL.md`(117줄) + `references/`(catalog.md 50명표 / harnesses.md 12레시피+토글 / agent-prompts.md 50개 전체 프롬프트, 실행 때 선발 id만 읽기). 참조는 scratchpad `build_refs.js`가 prompts.js·router.js·A배열에서 자동 생성. skill-creator 프로세스로 서브에이전트 3시나리오(리서치/온보딩/회의정리) 테스트→채점→개선. iter1 6.8→iter2~3 9.96. 핵심 개선: ①단계별 자기평가 줄 제거(점수 1회) ②검토 게이트를 적대적(목적·자료 대비 구체 결함부터, 실제로 떨어질 수 있게)→테스트서 9.44/[치명] 잡고 1회 보강 확인 ③웹툴 가능 시 리서치 단계가 실제 검색·출처(빈 [확인필요] 보고서 방지) ④이중 검토자(반박토글=실행단계 vs 게이트 검토자) 템플릿 ⑤린 팀(얇은 입력 단계 드롭) ⑥차원 5라벨 통일·반박토글 타이브레이커·지정 산출물 독립 블록. 정본은 루트 `roasting/`; `/roasting` 발동하려면 `.claude/skills/roasting/`로 복사+재시작 필요(미복사 상태).
- 2026-06-28 · v34 (수정10) · **팀 빌더(`#picker`)를 hero → #how 섹션 카드 아래로 이동**(사용자: "이거(#how 3단계 카드) 밑에 검색창·팀 리드 옮겨 넣자"). 흐름이 "설명(목적입력→팀장설계→복사 3카드) → 바로 그 자리 팀 빌더"로 자연스러워짐. hero는 헤드라인+CTA(팀원 만나보기/GitHub)만 남음. picker 마크업을 hero `.wrap`에서 제거하고 #how `.how` 카드 그리드 직후 `.wrap` 안에 그대로 삽입(전역 id #picker/#tbCatChips/#tbGoBtn 유지라 initBuilder 그대로 작동). #how는 `sec dark`(녹색)지만 `.picker` 컬러블록 스킨이 bg #fff !important라 녹색 위 흰 박스로 깔끔. nav '팀 구성'→#how 그대로(이제 설명+빌더 한 섹션). 검증: preview pickerInHow true·heroHasPicker false·칩4·칩→추천3→모달(조직도3셀)·pickerBg #fff, 콘솔0, 문법OK, picker 1개만 존재. (hero에서 빌더로 가는 CTA는 미추가 — 필요시 hero 버튼 href #how로.)
- 2026-06-28 · v34 (수정9) · **예시 메뉴·섹션 삭제 + nav 2개로**(사용자 지시). `#workflow` 섹션 전체 삭제 + `renderWF()` 함수·호출 제거(`#wfSteps` 참조 0). nav를 3개→**2개**: `팀 구성`(→#how), `팀원 50명`(→#catalog). `#how` 키커 라벨 `함께 일하는 법`→`팀 구성`(섹션 내용=목적입력→팀장설계→복사 흐름이 곧 팀 구성). 푸터 둘러보기 링크도 `팀 구성`+`팀원 50명`(예시 제거). 매핑 근거: 고아 섹션 안 생기게 nav 2개를 남은 콘텐츠 섹션 2개(#how, #catalog)에 연결. main 섹션=hero/how/catalog/shift-band(인용 밴드, 메뉴 외). 검증: DOM nav 2개·footNav 2개·#workflow/wfSteps 없음·howKicker '팀 구성', 콘솔0, 문법OK, 동기화. (.wf/.wf-steps CSS는 미사용으로 잔존, 무해.)
- 2026-06-28 · v34 (수정8) · **팀 프롬프트(오케스트레이션 스캐폴드) 평가→개선**(사용자: skill-creator로 10점 평가 후 개선). skill-creator의 평가/개선 철학(초안→비평→개선, 왜 설명, 군더더기 제거, 경직 MUST 지양)을 buildPrompt 생성물에 적용(설치형 SKILL.md 벤치 하니스는 부적합). RED 6차원 BEFORE 6.1/10 → 개선 후 9.5. 고친 것: ① `## 진행 방법` 4항 추가(한 LLM이 역할 순차 수행·단계별 산출물 드러내기·건너뛰기 금지·검토 실패 시 한 번 되돌려 통과) = 실행 프로토콜 명확화 ② 끝에 **입력 슬롯 안내**("이 줄 아래에 목적·주제·자료 붙여 넣고 시작") + 정보 부족 시 추측 말고 먼저 묻기 ③ 팀원 프롬프트 임베딩 시 맨 앞 H1 제거(`roleBody`가 `/^#.../` strip) → 이중 제목·계층깨짐 해소 ④ 헤더 `### N단계 · 직책 (en)` ⑤ 종결 통일: router.js 12개 lead+desc의 한다체 전부 합니다체로(perl 일괄: 만든다→만듭니다 등 19종), 스캐폴드도 합니다체. 검증: preview 생성물 재추출—doubleH1 0·입력슬롯 有·스캐폴드 종결혼용 false·본문 헤더+지침 정상, 콘솔0, JS 문법OK. router.js 루트·빌드 동기화. (팀원 50개 프롬프트 내부 한다체 리스트는 별도 자산이라 범위 외.)
- 2026-06-28 · v34 (수정7) · 조직도 **하단 수렴 버스 깨짐 수정**(사용자: 4명일 때 깨짐). 원인: 상단 버스는 `.tb-cell`(인접 붙음)에 그렸는데 하단 버스를 `.tb-cnode`(카드)에 그려 셀 좌우 패딩만큼 끊김. 해결: 수직 드롭은 `.tb-cell::before/::after`(셀 중앙 top/bottom 16px 막대)로, **분기/수렴 버스는 별도 엘리먼트 `.tb-chart-bus`(top/bottom) 하나씩**으로 분리하고 JS에서 팀원수 N 받아 인라인 `left=right=50%/N`(첫 셀 중앙~끝 셀 중앙)으로 정확히 그림. N=1이면 버스 생략. border 기반 per-card 버스 CSS 전부 제거. 검증: preview 4명("회의→실행 정리") 상·하 버스 안 끊김(스크린샷), 토글 +1→5명 buses inset 12.5%→10% 자동 재계산·buses 2 유지, 콘솔0.
- 2026-06-28 · v34 (수정6) · 모달 조직도를 **진짜 조직도(상위→가로 병렬→하단 수렴)로** 재교체(사용자: "리드 밑에 3개 병렬, 리뷰 맨 하단, 가로 줄여도 조직도로"). 수정5의 좌측 세로 트리(.tb-org--tree)를 폐기하고 **.tb-org--chart**: LEAD(풀폭) → `.tb-stem`(중앙 수직) → `.tb-chart-row`(flex, 팀원 `.tb-cell`>`.tb-cnode` 가로 병렬, flex:1 1 0·min-width:0로 폭 줄여 한 줄) → `.tb-stem` → REVIEW(풀폭). 상단 분기 버스+드롭=`.tb-cell::before/::after`(border-top 양쪽 반 + ::after border-left 중앙 수직, first/last로 양끝 컷), 하단 수렴 버스+드롭=`.tb-cnode::before/::after`(border-bottom + ::before border-right 중앙 수직). renderOrgInto를 chart 구조로 재작성, openHarnessModal org 클래스 tree→chart. 좁은 화면 버그 수정: 좌460px 고정이 vw541서 우측 48px로 눌림→`@media(max-width:980px)` 하네스 모달을 1fr 세로 스택(조직도 위/프롬프트 아래)+modal-info border-right→bottom. 넓은 화면은 460px/1fr 좌우. 검증: preview vw541 stack(좌우 496 풀폭)·chart-row flex 3셀 병렬·스템2·REVIEW 하단, 토글 사실검증 3→5 재조립+추가배지2+프롬프트 9.9KB, 콘솔0, 스크린샷(LEAD→버스 분기 3병렬→버스 수렴→REVIEW).
- 2026-06-28 · v34 (수정5) · 모달 좌측을 **조직도(트리)로** 교체(사용자: "왼쪽 일직선 병렬 말고 조직도로, 카드 작게라도"). 기존 세로 파이프라인(.tb-org--v: LEAD↓단계↓REVIEW + 화살표)을 **트리**(.tb-org--tree)로: 팀장(LEAD) 머리 카드 아래 **좌측 세로 스파인 + 각 팀원·검토자로 가는 엘보 커넥터**(`.tb-tree-rail` ::before 스파인/::after 가로 틱, first-child는 -12px로 LEAD까지, last-child는 bottom:50%로 꼬리 컷). 팀원 카드는 컴팩트 `.tb-tnode`(번호+직책+io, 3px 보더·작은 그림자, 4색 순환), REVIEW는 옐로 마지막 가지. renderOrgInto를 tnode() 헬퍼로 트리 생성하게 재작성, openHarnessModal org 클래스 `tb-org--v`→`tb-org--tree`. 검증: preview—treeItems=팀원+검토(예: 3+1=4), rails 일치, REVIEW 항상 마지막, 토글 분석강화 on→4→5 재조립+프롬프트 갱신(8.2KB), 콘솔 에러0, 스크린샷(LEAD 머리+좌측 스파인+엘보로 가지 친 조직도).
- 2026-06-28 · v34 (수정4) · 레이아웃·로고 정리(사용자 지시). ① 빌더 STEP1 순서 교체: **분야로 찾기(카테고리 칩) 위로, 목적 입력은 맨 아래**(`.tb-inputblock` 점선 구분 + "또는 목적을 직접 입력" 라벨). picker-label "분야로 찾거나, 목적을 입력하면…"로 수정. ② **헤더 로고 주황 복원**: src `logo1-white.png`→`logo1-transparent.png`(주황 원형), `.brand-logo` 검정 원 배경·radius·padding·shadow 전부 제거(투명·테두리 없음). ③ **푸터 로고 흰색**: fb-logo src `logo1-transparent`→`logo1-white.png`, 흰 배경·3px 테두리·그림자 제거. ④ **푸터 하단 바** 재구성: 좌 `© 2026 AI ROASTING. ALL RIGHTS RESERVED.` / 우 `강정구 · JAYDEN KANG` + LinkedIn·Facebook 인라인 SVG(흰색, hover 옐로). `.foot-bottom` 글자색 **흰색**(#fff). 소셜 href는 실제 URL 미상이라 `#` 자리표시자(지어내지 않음). 검증: preview 계산스타일—헤더로고 src/ bg transparent·radius0·shadow none, 푸터로고 src white·bg transparent·border0, builderOrder=[label,cats-label,chips,recos,inputblock], foot-bottom color #fff·credit·소셜2, 콘솔 에러0, 헤더 스크린샷(주황 원형 로고·검정원 사라짐).
- 2026-06-28 · v34 (수정3) · **하네스 카드 클릭 → 모달**로 전환(사용자: "각각 클릭하면 모달, 왼쪽 조직도·오른쪽 프롬프트, 위에 복사하기·MD 다운로드"). 인라인 STEP2(#tbStep2) 제거하고 기존 `#modal` 재사용: 모달 좌측 `.modal-info`를 `#modalAgentInfo`(에이전트용)+`#modalOrg`(하네스용)로 분기, `openHarnessModal(h)`가 좌=세로 조직도(`.tb-org--v` LEAD↓단계↓REVIEW)+토글, 우=`#modalBody`에 팀 프롬프트 박스+`복사하기`·`.md 다운로드`(`harnessMd`). 토글 클릭 시 조직도+프롬프트 동시 갱신(refresh 클로저). 모달 폭 1060·그리드 380px/1fr(`.modal--harness`). 노드 클릭→`openModal`(에이전트)로 같은 모달서 멤버 상세 전환(openModal이 modalAgentInfo 복구·modalOrg 숨김·modal--harness 제거). `renderOrg`→`renderOrgInto(host,h)`, `renderToggles`→`renderTogglesInto(host,h,onChange)`로 일반화, `openHarness`/STEP2 헤더·액션 마크업·initBuilder의 tbBack/tbCopyBtn 제거. 검증: preview로 모달 open·좌org(LEAD/노드/REVIEW)·토글4·우 복사·MD 버튼·프롬프트 6.6KB·토글 재조립(3→4)+프롬프트 갱신·복사 정상·노드→에이전트 전환·콘솔 에러0·스크린샷(좌 세로 조직도/우 프롬프트+2버튼).
- 2026-06-28 · v34 (수정2) · 빌더 카드 **마우스오버 효과 제거**(사용자: "마우스 오버 애니메이션 이상해"). 원인: `.tb-*`에 `position:relative`·`overflow:hidden` 없어 잉크 물결(`.hripple`)·리플 원이 카드에 고정·클립 안 돼 영역 밖으로 번짐. 수정: pointerover·pointerdown 핸들러 셀렉터에서 `.tb-*` 제거(네오브루탈 눌림 transform만 유지). 카탈로그 50카드 리플은 그대로.
- 2026-06-28 · v34 (수정1) · 팀 빌더 **안쪽 패딩 복구**(사용자: "여백 없이 이상해"). 옛 `.picker-top`/`.picker-body`(패딩 담당)를 `.tb-step`으로 교체하며 패딩이 빠져 내용이 4px 보더에 붙던 문제. `.tb-step{padding:26px 28px}`(모바일 18/16) 추가.
- 2026-06-28 · v34 · **팀 빌더(라우터) 신설 + 3층 스택 아키텍처 채택.** 사용자: "목적을 입력하면 팀원이 선발돼 워크플로우 포함 팀 구조가 설계되게". 브레인스토밍 결정: 엔진=하이브리드(즉시 미리보기 클라이언트 규칙 + 실제 실행은 복사 프롬프트), 입력=자유 입력+카테고리 칩 폴백, 출력=**구조형 팀**(LEAD 팀장 전용노드 + 단계 + REVIEW 검토자). 카탈로그 단위=**하네스(팀 레시피)**, 부품 50 재사용. 외부 repo(revfactory/harness-100=참고 메뉴, claude-plugins=패키징 포맷, launch-your-agent/CMA=런칭, ECC=품질 참고)는 **병합 아니라 스택**으로 PROJECT.md 3층 모델에 매핑(PLAN.md 정본). **범위 = [1] 라우터·팀설계 + [2] 복사 export까지**, 플러그인·CMA·런칭은 로드맵. 구현: `assets/router.js` 신설(HARNESS 시드 12 + MODIFIERS 토글4 + 카테고리4, 전역 데이터, 동기 `<script src>`로 prompts.js 다음 로드=file:// 안전). 렌더 로직은 인라인 IIFE(CLAUDE.md §4): 자유입력→키워드 점수순 추천(rankHarness), 선택→구조형 조직도(renderOrg: LEAD 핑크/단계 노드/REVIEW 옐로 + 커넥터·화살표), 토글→팀 즉시 재조립(partsList: fact=팩트체커+출처검증, deep=하네스별 deepAdd, rebut=비판적검토, trans=번역), `⧉ 팀 프롬프트 복사`→오케스트레이션 메타프롬프트(목적+팀장지침+각 부품 PROMPTS+handoff+산출형식, team 객체서 생성=export 훅). 기존 WORKFLOWS/renderGoals/renderTeam 제거, renderWF는 HARNESS[0]로 repoint, 히어로·how 카피 입력 흐름으로 수정, 리플 셀렉터 .tb-* 추가. 동기화: v34.html→두 index.html, router.js→루트·빌드 assets 두 곳. prompts.js는 v33 그대로. 검증: preview 서버로 globals(PROMPTS 50·HARNESS 12·칩4)·매칭·구조형 렌더(LEAD/4노드/REVIEW)·토글 재조립(7명·추가배지3)·복사 13KB(50프롬프트 포함)·콘솔 에러0·카탈로그 50·계산스타일(LEAD #FE90E8·4px·8px, 노드 178px·3px, REVIEW #F7CB46) 전부 확인. 합격선 9.5.
- 2026-06-28 · v33 · 프롬프트 루브릭을 **공통 5차원으로 통일**(역할 고유 6번째 차원 제거). 사용자: "50개 전원을 5개로 평가 기준으로 하자". v32에서 넣은 6번째(역할별) 차원을 빼고 5차원(정확성·근거/목적·완결성/구조·형식/실행력/언어·톤)만 50개 동일 적용. 총점=5평균, 자기평가 줄도 5항목. 위험 역할 안전장치는 본문 워크플로우·원칙에 그대로라 회귀 없음. 원본 덤프에서 `apply_rubric.py`(5차원판) 재생성 → `assets/prompts.js`만 변경, v33.html=v31 사본, prompts.js 두 assets 동기화. `_workspace` 평가기록도 5차원판으로 재작성(평균 9.79, 50/50 합격). 검증: node로 5차원·6차원 잔존 0·한자 0·em dash 0·자기평가 5항목.
- 2026-06-28 · v32 · 50개 프롬프트 루브릭 10점화 + 한자 제거 + `_workspace` 평가기록. 사용자: "루브릭을 10점 만점 상세 평가, 각 역할 10점 평가, 점수 남기기 / 한자 빼기 / `_workspace`에 평가기록". 설계(브레인스토밍 합의): A안=프롬프트 안 루브릭 개선, 공통 5차원×0~10 + **역할 고유 6번째 차원**(50개 각각 정의), 총점=6평균, 9.5 미만 자가수정, 산출물 끝 `자기평가:` 줄. 각 프롬프트의 `## 품질 기준 (Rubric)`(합격/불합격 체크) → `## 품질 기준 (10점 만점)`(6차원)으로 교체. 한자 4자(`上中下比`) → 상/중/하·대비. em dash 0 유지(루브릭은 콜론 사용). `assets/prompts.js`만 변경(HTML 불변), v32.html=v31 사본으로 버전 마킹, prompts.js 두 assets 동기화. `_workspace/2026-06-28_프롬프트-루브릭-평가.md`에 6차원 정의+역할별 6번째 차원 표(50)+50×6 RED 정적 채점표(평균 9.79, 50/50 합격, 차원평균 기록) 저장. 생성=scratchpad `apply_rubric.py`(루브릭 교체·한자) + `gen_report.py`(평가기록, 신호 키워드 유무로 재현가능 채점). 검증: node로 두 prompts.js 50키·결함0·한자0, em dash 0. 사용자: "인덱스 파일이 너무 커서 고민 → 별도 파일 하나로 빼자". `var PROMPTS={...}`(line 914~965, 148KB) 블록을 `assets/prompts.js`로 그대로 추출하고, 원래 자리에서 제거 후 메인 `<script>`(line 858) 바로 앞에 `<script src="assets/prompts.js"></script>` 삽입(동기 로드라 IIFE 실행 전 전역 PROMPTS 정의됨, 클로저 안에서 전역 참조). **fetch+json 안 씀**(file:// CORS로 막혀 더블클릭 시 프롬프트 전멸하기 때문). 결과: index.html 236379→88295B, prompts.js 148126B. prompts.js를 루트·빌드 `assets/` 두 곳에 동일 배치. 검증: node eval로 PROMPTS 키 50개·유효 JS 확인, HTTP 200(index/prompts.js/빌드경로 전부)·script태그 존재·인라인 PROMPTS 0·`PROMPTS[` 사용처 2곳 유지. (추출은 scratchpad가 아닌 python 인라인 스크립트, 바이트 단위 검증 완료.) `.agent-sec`(plain 텍스트)에 `border-top:2px solid #000`+`padding-top:12px` 재추가, `align-self:flex-start` 제거(기본 stretch로 콘텐츠 전폭). 검증: borderTop 2px 검정·pad 12·폭 430, 스크린샷(설명과 섹션명 사이 검정 선).
- 2026-06-28 · v30 · 번호배지 색·섹션 라벨 복원·X버튼 위치·로고 흰색. (1) **카테고리색을 섹션라벨→번호배지로 이동**: renderGrid에서 `.agent-no`에 인라인 `background:SECPAL[a[3]%8]`, `.agent-sec`는 인라인 색 제거+CSS를 plain 텍스트로 복원(box/border/radius 제거, `display:block;font-weight:700;background:transparent;border:none`). 번호배지는 섹션색+검정테두리, 섹션명은 흑색 텍스트. (2) **X 원문버튼 위치**: `.attr` `justify-content:space-between→flex-start`(이름 바로 오른쪽), `.quote-x` font 12.5→**10.5px**(2pt↓)+padding 축소. (3) **헤더 로고 흰색**: src `logo1-transparent.png→logo1-white.png`(빌드 assets에도 복사). 흰 로고는 투명배경 순백이라 크림 헤더서 안 보임 → `.brand-logo` 배경을 `#0a0a0a` 검정 원형(`border-radius:50%`)+`object-fit:contain`+padding 6으로 가시화(테두리는 없음=요청 "테두리 없애고" 충족, 다크 배지는 흰 로고 가시화 위한 불가피한 선택). 검증: 번호bg 핑크/민트(섹션별)·섹션 plain·attr flex-start·qx 10.5px·로고bg #0a0a0a, 스크린샷(로고 검정원형 흰로고, 카드 번호색, X버튼 이름 옆). `/compact`는 하네스 명령이라 미실행.
- 2026-06-28 · v30 · 결과 박스·섹션 라벨을 **각진 네모 박스로**(필 둥글기 해제) + 결과 박스 그림자 제거. 사용자: "그림자 없애자/네모난 걸로/선은 복원하자". `.cat-result-pill` `border-radius:100px→0`·`box-shadow:none`(테두리 var(--nb-b3) 유지), `b` 카운트 배지도 radius 0. `.agent-sec` `border-radius:0!important`(테두리·카테고리색 유지). "중간 선도 없애자"=설명과 섹션 사이 검정 선은 섹션을 필로 바꿀 때 `border-top` 제거돼 이미 없음(현재 카드 선은 아바타 회색 divider + 박스 자체 테두리뿐). 검증: 결과 radius0·shadow none·border3px, 섹션 radius0·border2px, 스크린샷(네모 박스, 검정 중간선 없음).
- 2026-06-28 · v30 · 카드 하단 섹션 라벨(`.agent-sec`)을 **카테고리색 필버튼**으로. 앞 `.sd` 사각점 `display:none`+마크업서 span 제거, `.agent-sec`를 inline-flex 둥근 필(`border-radius:100px!important`로 네오 radius0 덮음, 검정 2px 보더, 검정 글자, padding 4/14)로 바꾸고 배경은 인라인 `SECPAL[a[3]%8]`(섹션색). 기존 검정 윗선(`border-top`) 제거. 검증: card01 핑크·08 블루·14 그린·radius 100·사각점 없음, 스크린샷 핑크 필.
- 2026-06-28 · v30 · 카드 테두리 카테고리색 **롤백**(사용자 "이상하다, 검정 박스로 복원"). 카드 생성부의 `c.style.setProperty('border-color',...)` 한 줄 제거 → `.agent{border:var(--nb-b)!important}`(검정 4px) 복원. SECPAL 공유 변수는 결과 필버튼용으로 유지. 검증: card01/08/14 전부 rgb(0,0,0)·4px·인라인색 없음, 스크린샷 검정 박스. (직전 시도: 카드 테두리를 SECPAL 섹션색으로 인라인 !important 지정했었으나 롤백.)
- 2026-06-28 · v30 · 카테고리 결과 필버튼 + 헤더/필터바 스티키 스왑. (1) **결과 필버튼**: 필터바와 그리드 사이에 `<div class="cat-result" id="catResult">` 추가, renderGrid에서 `<span class="cat-result-pill">{카테고리명}<b>{개수}명</b></span>`(둥근 필 + 검정 카운트 배지) 채움. 배경은 **카테고리별 다른 색**(인라인): SECPAL `['#FE90E8','#C0F7FE','#99E885','#F7CB46','#FF9F9F','#C9A7FF','#6FE3CA','#FFC178']`를 섹션 0~7에 매핑, 전체=`#FFDC8B` 크림. 모두 파스텔이라 검정 글자·테두리 가독 OK. 전체=전체 팀원 50명, 섹션=섹션명+secCount, 검색 중=list.length. scrollToCards 앵커를 grid→catResult로 변경(클릭 시 결과 필버튼이 필터바 바로 아래 보이게). (2) **헤더 스티키 스왑**: 헤더 `position:static`→`sticky;top:0;z-60`, `.hdr-hidden{translateY(-100%)}`. `syncStickyHeader()`가 스크롤마다 카탈로그 섹션 rect로 필터바 stuck 여부 판정(`sec.top<=0 && sec.bottom>catH+20`) → stuck이면 헤더 숨김(필터바가 상단 차지), 아니면 헤더 스티키 노출. 검증: 모킹으로 상단=헤더보임/카탈로그안=헤더숨김/카탈로그아래=헤더보임, catResult "전체 팀원50명"→"재무·숫자6명", 활성칩 정확, 콘솔 0. (/compact는 하네스 명령이라 스킵.)
- 2026-06-28 · v30 · 카테고리 필터 클릭 시 카드 상단으로 스크롤 + GitHub 호버 확인. (1) **필터 클릭 스크롤**: `scrollToCards()` 추가(그리드 `#grid` top − sticky `.cat-controls` 높이 − 6 = 스티키 필터바 바로 아래로 `window.scrollTo({behavior:smooth})`, reduced-motion이면 auto). `allPill`/섹션 칩 onclick에 `renderGrid();scrollToCards();` 연결. 검증: 재무·숫자 클릭 → 카드 50→6 필터 + scrollTo({top:2341}) 호출·에러0. (2) **헤더 GitHub(#dlBtn) 호버**: 사용자가 "옆 예시 메뉴와 같은 노란 박스로" 요청했는데, 강제 호버 computed 확인 결과 **이미 동일**(옐로 배경+`3px 3px 0 #000` 그림자+border transparent, 팝아웃 없음 = `.nav-links a:hover`와 같음). 코드 변경 없음. 사용자가 본 검정 테두리 박스는 옛 캐시/미배포 버전(#dlBtn2 히어로 버튼 스타일)으로 추정 → 하드 리프레시/재배포 안내.
- 2026-06-28 · v30 · 아바타 1.5배 + 하단 회색 선 + 영문 역할명 노랑. `.agent-av` 96→**144px**. 하단 회색 선은 처음 이미지 폭만큼(`border-bottom`)이었다가 "카드 박스 전체 가로폭으로" 요청 → 아바타 뒤에 `<div class="agent-divider">` 삽입, `.agent-divider{border-top:2px solid #e4e4e4;margin:-6px -20px 16px;align-self:stretch}`(패딩 20px까지 가로질러 테두리에 닿는 full-bleed, 검증 divider 451=카드 459-보더8). 이미지와 선 사이 여백 제거 요청 → 아바타 `margin-bottom:0` + divider `margin-top:-6px`(flex `gap:6px` 상쇄)로 **선이 이미지 끝점에 정확히 닿음**(검증 divTop=avBottom, gap 0). `.agent-en` 색 `#000`→노랑. 흰 카드 위 순노랑(#F7CB46)은 대비가 낮아 "안 보인다" 피드백 → **다크 골드 `#b8860b`·weight 800**으로 진하게(노란 계열 유지하며 가독성 확보). 검증: DOM(av 144·border-bottom 2px #e4e4e4 / en rgb(247,203,70)) + 스크린샷.
- 2026-06-28 · v30 · 도메인 확정 + 아바타·필터바 다듬기. (1) **도메인 `50agents.vercel.app` 확정**: `og:url` 메타 추가, `og:title`의 em dash("에이전트팀 빌더 — AI ROASTING")를 "에이전트 팀 빌더 | AI ROASTING"으로 교체. (2) **스티키 필터바(`.cat-controls`) 위·아래 검정 선 제거**: `border-top/bottom:var(--nb-b)` → none(아래 회색은 칩 가로 스크롤바라 남음). (3) **아바타 키우고/테두리 제거/가운데**: `.agent-av` 64→96px, `border:none·box-shadow:none`, `align-self:center`. (4) **배지 잔상 제거 재크롭**: 64→96px·무테로 키우니 sample 좌상단 No 배지 노란 잔상이 보여, 크롭 박스를 오른쪽으로 이동(x0=[102,457,812,1167], y=[228,648], 232 정사각)해 배지 배제, char-1~8.png 재생성(양쪽). 검증: DOM(av 96·border0·center / cat border0 / og:url) + 카탈로그 스크린샷(아바타 크고 가운데·무테·배지 없음·필터바 선 없음), 콘솔 0.
- 2026-06-28 · v30 · **카드에 섹션별 캐릭터 아바타 8개 추가 + team-node 80% 축소 + 히어로 버튼 순서·테두리**. (1) **캐릭터**: `assets/agents/sample.png`(1448×1086, 8캐릭터 4×2 카드 스크린샷)에서 PIL로 8개 포트레이트를 cover 정사각 256px로 크롭 → `assets/agents/char-1.png`~`char-8.png`(루트+빌드 양쪽). 좌표: 열 x=[55-345,410-700,765-1055,1120-1410], 행 y=[행1 218-458 / 행2 648-888](룰러 크롭으로 얼굴 위치 측정: 행1 얼굴 y210-450, 행2 y635-920). renderGrid에서 `<img class="agent-av" src="assets/agents/char-{section+1}.png">` 추가(섹션 0~7 → char-1~8, 같은 섹션 카드는 동일 캐릭터). `.agent-av` 네오 스타일(64px·3px검정보더·3px오프셋그림자·정사각·좌상단). 50개 전부 256×256 로드 확인. (2) **team-node 80%**: 폭 200→160px(`flex/width`), padding 13/14→11/12(렌더 154px). (3) **히어로 CTA**: `.hero-cta .btn-ghost{order:-1}`로 "팀원 만나보기"를 GitHub 왼쪽으로(시각 순서 교체), 두 버튼(`#dlBtn2`,`.btn-ghost`)에 `border:3px solid #000` 검정 테두리. 검증: 카탈로그 스크린샷(아바타 프레임+섹션별 동일 캐릭터·흰 카드), DOM(아바타 src·border·50개 로드, ghost order -1·양 버튼 검정 보더, team-node 154px). **프리뷰 주의**: 이 환경은 JS 스크롤(window.scrollTo/scrollIntoView)이 안 먹고 `loading=lazy` 이미지가 화면 밖이면 안 떠서, 검증 시 `body transform:translateY`로 강제 노출하거나 src 재할당으로 강제 로드해야 함.
- 2026-06-28 · v30 · 풀쿼트 인용 출처에 **X 원문 출처 필버튼** 추가 + em dash 제거. `.attr`를 flex(`justify-content:space-between`)로 바꿔 이름(`.attr-name` 사티아 나델라, Microsoft CEO)은 왼쪽, 오른쪽에 둥근 `.quote-x` 필버튼(검정 bg·흰 X 로고 SVG·"원문 보기"·`border-radius:100px!important`로 네오 radius0 덮음·호버 시 흰 반전+3px 그림자). 링크=`https://x.com/satyanadella`(target=_blank). 인용문은 나델라 2026-06-14 X 에세이 "A frontier without an ecosystem is not stable"(휴먼 캐피탈+토큰 캐피탈 학습 루프). **정확한 게시물 URL은 웹검색·기사에서 직접 링크를 못 찾아 공식 X 프로필로 링크함**(지어내지 않음, 사용자가 정확한 포스트 URL 있으면 교체 권고). 기존 "— 사티아 나델라"의 줄표 제거. 검증: quote-x href·target·radius100·flex space-between·스크린샷(필버튼 우측 정렬) 확인, 콘솔 0.
- 2026-06-28 · v30 · 헤더 GitHub 호버 통일 + 워크플로우 노드 폭 통일. (1) **헤더 GitHub(`#dlBtn`) 호버를 다른 내비 메뉴와 동일하게**: 기존엔 `검정 보더 + 4px 그림자 + translate(팝아웃)`이라 plain 텍스트 내비(`.nav-links a:hover` = 옐로 배경 + 3px 그림자, 팝아웃 없음)와 달랐음. `#dlBtn:hover/:active`를 분리해 `옐로 배경 + 3px 그림자, border transparent, transform none`으로 맞춤. 히어로 GitHub(`#dlBtn2`)는 박스 팝아웃 애니메이션 유지(그룹 분리). (2) **워크플로우 team-node 폭 전부 통일**: 내용 길이에 따라 제각각이던 폭을 `flex:0 0 200px;width:200px;min-width:0`으로 고정(렌더 192px 균일), `white-space:normal`로 긴 설명은 2줄 wrap 허용(사용자: "아래 두줄 되어도 돼"). 검증: eval로 `#dlBtn:hover` 규칙=내비와 동일·team-node 5개 전부 192px 동일 확인, team-flow 스크린샷에서 2번째 카드 설명 2줄 wrap 확인, 콘솔 0.
- 2026-06-28 · v30 · 히어로 "팀원 만나보기"(`.btn-ghost`) 검정 박스 테두리·그림자 제거. 히어로 GitHub 버튼(`#dlBtn2`)은 이미 플랫인데 ghost만 흰 배경+검정 네오 박스라 둘이 안 맞았음 → `.btn-ghost`를 `#dlBtn2`와 동일 패턴(기본 transparent 배경·3px transparent 보더·그림자 0, 호버 때만 옐로 박스+오프셋 그림자+translate)으로 통일. 검증: preview eval로 background/borderColor transparent·boxShadow none 확인, 히어로 스크린샷에서 두 버튼 모두 플랫. (사용자 "v24.html 디자인으로 v30" 지시는 v24=옛 람보르기니 라이트 확인 후 "하지 마"로 철회 → v30은 네오 디자인 유지.)
- 2026-06-28 · v29 · **프롬프트 마크다운 재정리 + Opus 4.8 가이드 10점 + 메인 디자인 v25 적용**. 사용자: ① Opus 4.8 전용 가이드(`prompting-claude-opus-4-8`)를 실제로 봤냐 지적 → 솔직히 그 전엔 일반 가이드만 봤음을 인정하고 해당 페이지 정독. 반영: 사고 기본 off라 "답하기 전에 차근차근 생각" 명시, 리터럴 지시(범위 명시), 긍정 지시 우선, 도구는 언제·어떻게 명확히. ② "마크다운 형식으로 재정리" → XML 태그(`<목표>` 등) → 마크다운 헤딩(`# 제목`, `## 핵심 역할`/`## 도구`/`## 워크플로우`/`## 출력 형식`/`## 예시`/`## 품질 기준 (Rubric)`/`## 원칙`)으로 전환(harness-100 analyst.md 스타일). ③ "메인 디자인은 v25.html로 적용" → v25.html을 디자인 베이스로 삼아 프롬프트만 주입(직전 v28에 v27/v28 디자인 재적용하던 작업은 폐기). em dash 0(프롬프트), 평균 1316자. **부수 버그픽스**: v25에 남아 있던 다운로드 버튼 id 충돌(`dlBtn`, 헤더 GitHub와 겹침)을 `mdDlBtn`으로 분리(v26에서 고쳤으나 v25 베이스라 재적용 필요). 생성=`gen_md.py`(gen10 D + gen_pro E import, `__main__` 가드). 검증: preview eval로 마크다운 7개 ## 헤딩·제목·받음/전달 핸드오프·"답하기 전에"·번역투 지시·em dash 0·복사(1304자)·다운로드(agent-08-data-analyst.md, 실제 모달 버튼)·콘솔 0·스크린샷(v25 네오 디자인+마크다운 렌더) 확인.
- ⚠️ 사고: v27.html/v28.html은 병렬 디자인 작업분이었는데 내가 cp로 덮어쓴 적 있음(복구 불가). 이번엔 v25를 명시 베이스로 써서 회피. 앞으로 새 버전은 항상 **최신 배포본을 cp**하고, 디자인/프롬프트 동시 편집 시 어느 파일이 정본인지 먼저 확인할 것.
- 2026-06-28 · v28 · **레이아웃 정렬 + 군더더기 제거 5건**. 사용자 연속 지시. (1) **목표 칩(.goal-chip) 가로 동일폭**: `.goals` flex nowrap + 칩 `flex:1 1 0;min-width:0;text-align:center;white-space:nowrap`(5칸 균등, 1280에서 각 213px·1행). 모바일(≤760)은 wrap+40%. (2) **워크플로우 노드(.team-node) 가로 동일폭**: `flex:1 1 0;min-width:0`(min-width:150 제거), 모바일은 `flex:0 0 200px`. 1280에서 각 187px·1행. (3) **히어로 '팀원 만나보기' 버튼(.btn-ghost) 검정 박스 제거**: border/box-shadow/배경 none, 호버 리프트(`transform:translate(-2px,-2px)`)·액티브는 유지(=애니메이션 그대로). btn-ghost는 이 버튼만 사용. (4) **eyebrow 앞 ■ 점 제거**: HTML에서 `<span class="pulse">` 삭제(.pulse는 이 한 곳만). (5) **상단 스티키 필터바(.cat-controls) 위·아래 검정 선 제거**: border-top/bottom `none`. 검증: preview 1280px eval로 칩 5개 213px·노드 5개 187px 균등·각 1행·텍스트 클립 0, ghost border0/shadow none/bg transparent, pulse 미존재, cat 보더 0, 콘솔 에러 0, 스크린샷 확인.
- 2026-06-28 · v27 · **카탈로그 카드(.agent) 호버 그림자를 검정 단색 → 5색 컬러 그림자로**. 사용자가 "마우스 오버하면 그림자 색깔을 원래 템플릿대로 다양한 색깔로 주자". 렌더 시(JS `renderGrid`) 카드별 CSS 변수 `--sh`를 팔레트 `_pal[(id+1)%5]`로 지정(배경 `_pal[(id-1)%5]`와 항상 2칸 오프셋이라 카드색≠그림자색). 권위 스킨 `.agent:hover` box-shadow를 `11px 11px 0 #000` → `11px 11px 0 var(--sh,#000)`. 기본(비호버) 그림자는 검정 8px 유지. 검증: preview eval로 카드01~06 `--sh`가 green/yellow/cream/pink/blue로 순환·배경과 모두 상이 확인, `var(--sh)` 실제 색 resolve 확인, `.agent:hover` 매치 룰 확인, 콘솔 에러 0.
- 2026-06-28 · v26 · **50개 프롬프트 10점 재작성(Anthropic 공식 가이드 기준) + 모달 다운로드 버튼 id 충돌 버그픽스**. 사용자가 클로드 프롬프트 가이드를 웹서치로 찾아 그 기준으로 올려달라 함. docs.anthropic.com/platform.claude.com 베스트프랙티스 정독 후 3대 적용: (1) **XML 태그 구조** — `[입력]` 대괄호 → `<목표>/<입력>/<결손_처리>/<작업_순서>/<출력_형식>/<예시>/<자가_점검>/<원칙>`(가이드: XML이 지시·맥락·예시 혼동을 줄임). (2) **few-shot 예시** — 50개 전부 `<예시>`에 입력→출력 골격 삽입(가이드 최강조: \"형식·톤·구조를 잡는 가장 신뢰할 수 있는 방법\"), 도메인 고착 줄이려 \"형식 참고용\" 명시. (3) **above-and-beyond + 사고 분리** — `<목표>`에 \"평범에서 멈추지 말라\", `<작업_순서>`에 \"과정은 출력 않고 결과만\"(think step by step + 깔끔한 출력). 직전 RED 지적도 해소: **[자가_점검]≠[원칙]**(점검=보내기 직전 함정 1회 검사 / 원칙=불변 규칙), **[작업_순서]≠[출력]**(순서=사고법 / 출력=목차). 평균 940자. 생성=scratchpad `gen10.py`(9필드 dict→XML 조립). **버그픽스**: 헤더 GitHub 버튼이 선재적으로 `id=\"dlBtn\"`을 쓰는데 모달 다운로드 버튼도 같은 id → `$=querySelector`가 첫 #dlBtn(헤더)을 잡아 **모달 .md 버튼 핸들러 미부착 + downloadMd가 GitHub 버튼에 붙음**, 헤더용 `#dlBtn{background:yellow!important}`까지 모달 버튼에 적용돼 파랑이 노랑으로 보였음. 모달 버튼 id를 `mdDlBtn`으로 분리(핸들러+색 동시 수정). v25부터 잠복. 검증: 실제 모달 버튼으로 다운로드(agent-14-financial-analyst.md·blob)·복사(1049자)·배경 rgb(192,247,254) 파랑·GitHub 핸들러 없음·XML 렌더+스크롤 스크린샷, 콘솔 0.
- 2026-06-28 · v25 · 그림자 정리 + 카드 흰 배경 + 가독성 5건. (1) **카탈로그 카드(.agent) 전부 흰 배경**(JS 5색 순환 inline 제거 + `background:#fff!important`) + **기본 그림자 제거, 호버 때만 그림자**(`.agent` box-shadow none, `.agent:hover` 11px 오프셋 유지). 검정 하드섀도 50장이 너무 산만하다는 피드백. (2) **필터 칩(.fpill) 그림자 제거**. (3) **팀 노드(.team-node) 그림자 제거**(기본+호버), 색상(블루/핑크/그린/옐로)은 유지. (4) **#how(함께 일하는 법) sec-desc 흰→검정**: 그린 배경에 흰 글자라 안 보이던 문제(`.sec.dark .sec-desc` 흰색이 이김) → `#how .sec-desc{color:#000!important}`. (5) **푸터 폰트 Pretendard**: foot-col h4·foot-bottom·colophon의 Space Grotesk 모노 해제(`footer ...{font-family:var(--sans)!important}`). 검증 DOM(카드 흰색·그림자 none·칩/노드 그림자 none·how 설명 검정·푸터 Pretendard), 콘솔 0.
- 2026-06-28 · v25 · 헤더 미니멀화 3건. (1) **헤더 5색 시그니처 스트립 제거**(`header::after`): 크림 배경에서 옐로·크림 밴드가 묻혀 "핑크 선"처럼 보였음. 푸터 스트립(`footer::before`, 검정 배경)은 유지. (2) **브랜드 로고 검정 사각 배지 테두리 제거**: `.brand-logo` border/bg/padding 0 → 원형 로고만. (3) **GitHub 버튼(헤더 #dlBtn·히어로 #dlBtn2) 배경 제거 + 호버 시에만 네오 박스 애니메이션**: 기본 transparent bg·transparent 3px 보더(레이아웃 점프 방지)·그림자 없음 → 호버 시 옐로 fill+검정 보더+4px 오프셋 그림자+translate 노출. 검증 DOM·헤더 스크린샷(스트립·로고 프레임·버튼 박스 모두 사라짐), 콘솔 0. (콘텐츠 폭 문의: 1200px 유지 결정.)
- 2026-06-28 · v25 · 히어로 핑크 사각형 데코 재배치 + 헤더 GitHub 버튼 플랫화. (1) `.hero::before`(핑크 회전 사각형)을 top:46/right:6%/120px → **top:20/right:3.5%/92px·rotate 9°**로 우상단 코너에 올려 헤드라인과 안 겹치게(≤1000px에선 72px). (2) 헤더 GitHub 버튼(`#dlBtn`)의 진한 네오 박스(4px보더+오프셋그림자) 제거 → **플랫 옐로**(보더/그림자/호버 transform 0, 호버 밑줄만). 히어로 GitHub(`#dlBtn2`)는 네오 박스 유지. (3) 콘텐츠 가로폭 문의: 현재 **1200px**(1080 아님), 사용자 확인 후 1200 유지. 검증 DOM·헤더 스크린샷, 콘솔 0.
- 2026-06-28 · v25 · 헤더·푸터 네오브루탈리즘 통일. 헤더: 브랜드 900, 로고를 흰 배지(3px 검정 보더+흰 bg 타일)로, 내비 호버/활성=옐로 블록+하드섀도(밑줄 대신), 하단 4px 검정 보더 아래 **5색 시그니처 스트립**(`header::after` linear-gradient 핑크/블루/그린/옐로/크림). 푸터: 상단 동일 **5색 스트립**(`footer::before`, padding-top 64px로 자리 확보), fb-name 900·22px, 검정 배경에 안 보이던 로고를 **흰 타일**(bg #fff+3px 흰 보더+옐로 하드섀도)로, h4 옐로+옐로 밑줄, 링크 호버 옐로 밑줄. 검증: DOM(브랜드 900·로고 배지·헤더/푸터 그라데이션 스트립·흰 로고 타일·옐로 라벨·footer pad-top 64)·헤더 스크린샷, 콘솔 0. (푸터 스크린샷은 도구가 상단만 잡아 DOM으로 확인.)
- 2026-06-28 · v25 · **50개 프롬프트 전면 재작성(9.8점 목표) + 모달 좌우 2단 재설계**. (1) 프롬프트: 기존 [입력]/[결손]/[출력]/[원칙] 구조에 5-Color Harness 자가검증 철학 + skill-creator 원칙(왜를 설명·사고 절차 명시)을 더해 8요소 구조로 격상 — 첫 줄 **경계**(어떤 역할과 구분되는지=handoff), **왜/목표**, [입력], [결손], **[순서]**(역할별 사고 절차 5단계), [출력], **[점검]**(내보내기 전 자가검증), [원칙]. 50개 전부 역할 맞춤(보일러플레이트 금지). Python+`json.dumps`로 PROMPTS 객체 안전 치환. **함정**: `re.sub` 치환문자열은 `\n`을 실제 줄바꿈으로 해석 → 1차 결과가 깨짐 → `lambda m: js` 함수 치환으로 회피(앞으로 동일 패턴 주의). (2) 모달: 단일 컬럼 → `.modal-grid` 2단. 좌 `.modal-info`(아바타칸 숨김 유지 + No.·섹션·직책·영문·설명·메타칩) = **역할·설명**, 우 `.modal-main` = **프롬프트**. 프롬프트 박스 위에 `prompt-tools`로 **복사(옐로) + .md 다운로드(블루)** 2버튼(`pt-btn`, 4px보더+하드섀도). 신규 `downloadMd(id)`: Blob→임시 앵커 클릭, 파일명 `agent-NN-slug.md`, 본문=제목+메타+프롬프트 코드블록+안내. `copyText`는 아이콘·라벨 날아가던 `textContent` 방식을 `dataset.orig` 복원식으로 수정. 좌 크림패널+검정 우측보더, 720px↓ 1단 붕괴. 검증: 데스크톱·모바일 DOM·스크린샷·복사(클립보드)·다운로드(파일명·blob·토스트) 확인, 콘솔 에러 0.
- 2026-06-28 · v25 · 스티키 정리: **카테고리 필터 칩 바만 스티키**. 헤더 nav를 `position:static`(스크롤되어 사라짐)으로, 검색창은 `.cat-controls` 밖으로 빼서 스크롤되게, `.cat-controls`(이제 `#filters`만 포함)를 `position:sticky;top:0`으로 상단 고정. 검증: 헤더 static / cat-controls sticky top:0 / search 컨트롤 밖(relative) DOM 확인, 콘솔 0. (부수: `.claude/serve.js`의 `/`→`v19.html`(옛 버전)을 `/index.html`로, `PORT`를 env 사용으로 수정 + `launch.json autoPort:true`. 그간 "/"가 옛 화면 보이던 원인이 이 stale 기본 경로였음.)
- 2026-06-28 · v25 · **전면 리디자인: 컬러 블록 네오브루탈리즘**. 사용자 지시("과감하게 변경" + 컬러 블록 스펙)로 람보르기니 라이트 → 네오브루탈리즘 전환. 결정: 사이트 전체 리스킨 / 픽셀 아바타 제거 / 직각(radius 0) / Space Grotesk 추가(이미 import돼 있었음). 구현=A안(테마 CSS 권위 레이어 추가 + 카드/팀/모달 JS 보정, 데이터·로직 전부 유지). (1) `</head>` 직전 마지막 `<style>`에 5색+4px검정보더+8px오프셋그림자+직각 스킨 추가, `!important`로 이전 람보르기니 레이어 덮음. 람보르기니 `border-radius:*!important` 4줄은 0으로 변경(전역 `*{radius:0!important}`는 특이도상 클래스 라디우스를 못 이겨서). (2) 카드: 아바타 제거, JS에서 `(id-1)%5` 5색 순환 배경 inline, 번호 배지 01~50 흰 박스, 직책 900 디스플레이, 영문 모노. (3) 섹션 매핑: hero=cream+핑크 회전 사각형 데코, #how=green 블록+흰 카드, 풀쿼트=pink 밴드+흰 quote-frame+옐로 따옴표, 워크플로우=옐로 헤더+컬러 ws-dot, footer=black. (4) 모달=흰 카드 4px보더+14px그림자, 아바타 숨김, 옐로/핑크/블루 액센트, 프롬프트 박스 블랙. 검증: DOM으로 카드 50개(275×150 균일·핑크/블루 등·4px보더·8px그림자·radius 0·opacity 1·아바타 없음)·전 섹션 색·모달 확인, 콘솔 에러 0. (히어로·모달 스크린샷 정상, fold 아래는 미리보기 도구 한계로 캡처 불가). CLAUDE.md §4/§5/§6도 컬러 블록으로 갱신.
- 2026-06-28 · v24 · 디자인 시스템: 4모서리 라운드로 전환 + 카드 배지 필버튼 + 메뉴 순서. (1) **모서리 정책 변경**: 기존 비대칭(우상+좌하만 라운드 `16/11/7/5px 0 ...`) 오버라이드를 4모서리 대칭(`16px/11px/7px/5px`)으로 전부 교체. CLAUDE.md §4의 "0-radius 각진" 규칙과 다르나 사용자 지시로 전환(카드 .agent 포함 모든 사각형). (2) **카드 번호 필(pill) 버튼화**: `.agent-no`를 골드(#FFC000) 배경·블랙 글자·`border-radius:100px` 알약형으로, 텍스트는 `No.N`→2자리 `('0'+id).slice(-2)`로 01~50. (3) **카드 텍스트 배열 복원**: 직전에 넣은 2줄 고정 슬롯(line-clamp/min-height) 제거, 자연 흐름 복귀(카드 외곽 높이는 `grid-auto-rows:1fr`로 여전히 균일 225px). (4) **헤더·푸터 내비 순서 변경**: `팀원 50명, 함께 일하는 법` → `함께 일하는 법, 팀원 50명`. 예시(#workflow) 링크·섹션은 유지(사용자 확인).
- 2026-06-28 · v24 · 섹션 순서 교체 + 카드 템플릿 고정 + 미세 정리. (1) `함께 일하는 법`(#how)을 hero 바로 뒤로, 풀쿼트 밴드(`shift-band`)를 카탈로그 뒤로 이동. 키커 번호 재정렬: #how=01, #catalog=02, #workflow=03(앵커 id·내비 링크 불변). (2) 카드 크기·모양 완전 통일: `.agent-name`·`.agent-ko`에 2줄 고정 슬롯(`-webkit-line-clamp:2` + `min-height` 2.5em/2.9em) → 글자 길이 무관 전 카드 동일(검증 카드H 245px·아바타 69²·name 38·desc 36 단일값). 아바타는 기존 32뷰박스 2톤 배경 72px 타일 그대로. (3) 카탈로그 필터 선과 카드 사이 여백 복원: `.grid{margin-top:22px}`(catCount 삭제로 사라졌던 간격). (4) 푸터 문구 `판단은 5color가, 실행은 이곳이 맡습니다.` 삭제.
- 2026-06-28 · v24 · 카탈로그 헤더 정리: `01 팀원 카탈로그` 섹션의 설명 문구(`AI로 다시 그린 우리 조직도…`) `p.sec-desc` 삭제, 카드 수 카운트(`#catCount` "50명" 표시) div·JS 채우기 라인·`.cat-count` CSS 모두 제거. `h2.sec-h "50명의 에이전트 팀원"`은 유지. 콘솔 에러 0, 카드 50개 균일 유지.
- 2026-06-28 · v24 · 카탈로그 50 카드 높이 통일. `.grid`에 `grid-auto-rows:1fr` 추가 → 설명 문구 길이와 무관하게 전 카드 동일 높이(검증: 50개 전부 207px 단일값). 너비는 기존 auto-fill minmax로 이미 균등.
- 2026-06-27 · v22~v23 · **50개 프롬프트 RED 품질 루프 → 50/50 9.5 통과**. 병렬 RED 4명 채점(v21, 합격 0) → 결함 5종(결손 프로토콜 부재·표어 수준 안전장치·척도 미지정·인접 경계 미설정·재무 형식 미강제) 종합 → BLACK 전면 재작성(v22, `[결손]` 블록 의무화 + 역할별 가드/중단조건/분량/인접경계) → RED 2명 재채점(FAIL 12) → 12개 외과 보강(v23) → RED 최종 12/12 PASS. 위험역할 구체가드: 43 채용절차공정화법 금지항목, 42 발신자 긴급도 불신·발송금지, 45 비가역작업 승인게이트, 46 법령·판례 위조금지, 49 반증조건+치명도 등급. 라운드 근거는 `output/agent-team-builder/EVALUATION.md`. 주입 스크립트는 scratchpad(inject_v22.py 전체 교체 / inject_v23.py 12개 라인 패치).
- 2026-06-27 · v21 · **50명 전원 시스템 프롬프트 작성** — 기존 5개(1·8·20·22·28) 양식을 따라 나머지 45개를 추가, `PROMPTS` 객체를 50개로 교체. 양식 통일: `당신은 [역할]입니다 → 목표 한 줄 → [입력]/[도구] → [출력] 번호 목록 → [원칙](사실/추론 분리·지어낸 숫자 금지·~합니다 종결)`. 이제 모달이 전원 "셋업 가이드 준비 중" 폴백 대신 실제 프롬프트+복사 버튼 노출. 주입은 scratchpad `inject_prompts.py`(json.dumps로 JS 문자열 escape, 정규식으로 PROMPTS 블록 치환). node 파싱 검증 통과(`document` 런타임 에러만, 선언부 전부 정상). 별도 산출물: `guides/GAP_ANALYSIS_vs_harness100.md` 정본 저장.
- 2026-06-27 · v20 · 라인업 보강 — harness-100(revfactory) 100개 파이프라인 MECE 대비 갭 분석 후, 약하거나 중복인 6명을 **같은 섹션 인플레이스 교체**(총 50 유지). 삭제→추가: 17 투자검토→**IR·투자자 리포트**(재무), 26 기사작성가→**정부지원사업 작성가**(글쓰기), 29 액션아이템추적→**고객 응대(CS)**(커뮤니케이션), 37 우선순위정리→**제품 기획자(PRD)**(기획), 43 체크리스트→**채용 담당**(생산성), 49 학습요약→**비판적 검토자(Devil's Advocate, AI ROASTING DNA)**(검토·품질). 워크플로 "회의 끝나고 정리하기" [28,29,30]→[28,41,30]으로 dangling 참조 수정. 신규 6명은 PROMPTS 미작성 → 모달 "셋업 가이드 준비 중" 폴백(기존 ~45명과 동일). 의도적 제외: 소프트웨어개발·법률·헬스·부동산/이커머스/ESG 등 버티컬(범용 지식노동자 축 유지).
- 2026-06-27 · v19 · 캐릭터 생성기 v3 — **32그리드**로 상향, **림라이트/폼섀도 반투명 오버레이**(실루엣 안쪽 가장자리)로 입체감 부드럽게(레퍼런스 손맛 AA에 근접). **동양인**(따뜻한 밝은 피부4·흑발 위주6), **남녀 혼합**(짝수 id=여성 롱/단발/포니테일/번+속눈썹+입술, 홀수=남성 숏/가르마/슬릭/버즈+수염 일부). 카테고리별 옷색 8종 유지. 타일은 밝음·직각 유지. 시드 parity 붕괴 수정(`(id*7%13)%4`, `((id/2)|0)%4`). 사용자: 레퍼런스급 마스코트 퀄리티 요청 → "동일은 불가(장인 수작업 50× 불가), 근접까지" 합의 후 절차생성 최대치로 진행.
- 2026-06-27 · v18 · 픽셀 캐릭터 10점 마감 폴리시 — (1) 아프로(hs3) 볼륨 확대, (2) 두 눈 동공을 평행으로(시선 정면화), (3) 직무 소품을 좌상단 떠다니는 글리프 → **우하단 어깨에 단 역할 칩**(섹션 다크 7×7 배경 + 흰 픽토그램)으로 변경해 가독성↑·정돈감↑. RED 9.8.
- 2026-06-27 · v17 · 픽셀 캐릭터 프로페셔널 리메이크 — 18→**24그리드**, occupancy 맵 `M`에 레이어 페인트 후 **4-이웃 자동 외곽선** + **다톤 셰이딩**(피부/머리/옷 각 3톤, 명암 방향 통일). 섹션 팔레트를 SEC2(cb/cs/ch/l1/l2/d)로 확장, 타일은 2밴드(l1/l2). 카드 66px·모달 108px. **버그픽스: `hs=(id*3)%6`은 3·id가 0/3만 나와 헤어가 2종으로 붕괴 → `(id*17)%6`(6과 서로소)로 6종 전부 출현.** 눈=흰자+동공, 안경=라운드 프레임, 소품 엠블럼 재디자인.
- 2026-06-27 · v16 · 50명 픽셀 SVG 캐릭터 추가 — `pixelAvatar()` 파라메트릭 생성기(id 시드, 결정론적). 카드 이름 위 중앙 58px, 모달 헤더 94px. 섹션색 8종(SECC)으로 타일/옷, 얼굴은 중립 피부톤으로 개인 구분, 좌상단에 직무 소품 엠블럼(돋보기·막대·동전·펜·말풍선·깃발·톱니·체크). 호버 시 무드4종(bob/tilt/hop/sway)+눈 깜빡+소품 흔들, 모달은 상시 애니메이션. `prefers-reduced-motion` 정지. No.뱃지는 카드 우상단 코너로 이동.
- 2026-06-25 · v15p · 로고 옆 "AI ROASTING"을 볼드(700)로, 색도 한 단계 진하게(ink2).
- 2026-06-25 · v15o · 인용문 "학습의 고리를"→"학습의 루프를", 섹션 제목·설명(.sec-h/.sec-desc) max-width 해제 → 1200px 안에서 한 줄로(중간 끊김 해소).
- 2026-06-25 · v15n · 풀쿼트(.shift) 글자 잘림 수정 — 대각선 라운드 변경 때 .shift가 라운드 목록에서 빠져 되살아난 border-radius:24px+overflow로 모서리가 글자를 잘랐음. .shift border-radius:0·overflow:visible로 고정.
- 2026-06-25 · v15m · 풀쿼트 둘째 문장("일은, 나아가 직무까지…") 삭제, 첫 문장만 유지.
- 2026-06-25 · v15l · 풀쿼트를 원문 순서에 충실하게 재번역(진짜 기회는 최고 모델이 아니라 휴먼/토큰 캐피탈이 복리로 쌓이는 학습의 고리 → 일·직무는 넘겨도 배움은 못 넘김), 길어진 인용에 맞춰 .shift h2 폰트 살짝 축소·줄간격 조정.
- 2026-06-25 · v15k · 히어로 헤드라인을 "목표를 설정하면, 자동으로 에이전트 팀이 꾸려집니다"로 변경(강조: 에이전트 팀).
- 2026-06-25 · v15j · 풀쿼트 한국어를 더 자연스럽게 다듬음("일과 직무는 넘겨도, 배움은 넘길 수 없습니다 / 휴먼 캐피탈과 토큰 캐피탈은 함께 복리로 쌓입니다"), 박스 모서리를 **좌측 상단 + 우측 하단** 라운드(반대 대각선)로 변경.
- 2026-06-25 · v15i · 풀쿼트 제목(.shift h2) max-width 제한 해제 → 문장이 어절 중간에 안 끊기고 문장 단위로 줄바꿈.
- 2026-06-25 · v15h · 풀쿼트 밴드 문구를 "리더는 프롬프트를…" → **사티아 나델라(MS CEO)의 휴먼 캐피탈/토큰 캐피탈 발언 인용**으로 교체("일은 넘길 수 있어도 배움은 넘길 수 없습니다. 휴먼 캐피탈과 토큰 캐피탈은 함께 불어납니다"). 출처 검증 완료(Techmeme/Bearly AI 등 X 게시).
- 2026-06-25 · v15g · 모든 박스 모서리를 **비대칭 라운드(우측 상단 + 좌측 하단만 둥글게, 나머지 직각)**로 변경. 크기별 라운드값(카드 16 / 버튼 11 / 배지 7 / 카운트 5).
- 2026-06-24 · v15f · 한국어 줄바꿈을 **어절 단위(word-break:keep-all)**로 바꿔 단어 중간 끊김 해소("찾아보십시오" 같은 분리 방지), 푸터 콜로폰("이 지면은 AI ROASTING이 …") 삭제.
- 2026-06-24 · v15e · 풀쿼트 밴드 **풀폭**(shift-band 분리, 내용은 1200 중앙), 히어로 이어 문구 "**AI 에이전트를 고용하라**", 콘텐츠 폭 **1080→1200px**, 영어 전용 **Space Grotesk** 폰트 적용(한글은 Pretendard 유지), 브랜드 **글자 물결**(마우스 오버 시 글자별 출렁, 브랜드 리플은 제거).
- 2026-06-24 · v15d · 워크플로우 연속선 재수정(점 뒤 absolute 선으로 행 높이 무관하게 연결), 풀쿼트 통계 카드를 리더 관점으로 재작성(9.5+ 제거 → 50/8/0: 일 대신·8영역 연결·채용0), "스킬 다운로드" 버튼을 **GitHub 버튼(로고+↗, 새 탭)**으로 교체.
- 2026-06-24 · v15c · 다크 배너(#how) 다시 **풀폭(가로 전체)**으로 복원, 브랜드를 **"에이전트 팀 빌더 | AI ROASTING" 한 줄**로 표시, 브랜드 마우스 오버 시 물결 애니메이션 추가(로고 360° 회전 유지).
- 2026-06-24 · v15b · 워크플로우 "5명·약 20분" 메타 삭제, 다크 밴드(#how)를 풀폭→**콘텐츠 1080px 폭 블록**으로 정렬(가로 폭 어긋남 해소).
- 2026-06-24 · v15 · 람보르기니식 **다크/화이트 밴드 교차**(함께 일하는 법 섹션 + 푸터를 풀폭 블랙으로) 단조로움 제거, 워크플로우 스테퍼 **연속선**으로 수정(스텝 패딩·구분선 제거해 줄 끊김 해소).
- 2026-06-24 · v14 · 푸터 "실행은 여러분의 Claude…" 문구 삭제, 혼자/도구(A·B) 배지 전부 제거(카드·워크플로우·모달), 본문 가로 1120→**1080px**.
- 2026-06-24 · v13 · 람보르기니 **라이트 모드** 전환(흰 캔버스+골드+블랙 풀쿼트), 필터 버튼 **한 줄**(등폭), 브랜드 "에이전트 팀 빌더 | AI ROASTING", 마우스오버 물결·로고 360° 회전 추가.
- 2026-06-24 · v12 · 람보르기니 **다크 모드** 시안(블랙+골드). (라이트 요청으로 대체됨)
- 2026-06-24 · v11 · 50개를 **B2B 세일즈 → 범용 지식노동 8직군**으로 재구성, 필터 버튼에 **개수 카운트 pill** 추가, 검색/카피 범용화.
- 2026-06-24 · v10 · 로고 적용(원형 AI ROASTING), 헤더 밑 선 상시 표시, 히어로 헤드라인/서브카피 한 줄·드롭캡 제거, 헤드라인 폭=본문 폭, "AI 팀원→에이전트 팀원".
- 2026-06-24 · v9 · Pretendard 단일 폰트 전환 + PINK↔RED UIUX 루프(muted 대비 AA, 키커 칩, tabular-nums) → 9.5 합격.
- 2026-06-24 · v7 · 마우스다운 리플 + 매거진 에디토리얼(마스트헤드·키커·풀쿼트·드롭캡) → 10점. (이후 마스트헤드 삭제)
- 2026-06-24 · v6 · 톤 입니다체 통일, 50개 한국어 팀원 직책명, 작동방식/예시 평이화, 팀원 카탈로그를 상단으로 재배치.
- 2026-06-24 · v1~v5 · 초기 제작 + web-builder BLACK/RED 루프로 9.0→9.8까지 개선(히어로 라우터 데모·50 카탈로그·W1 워크플로우·모달·복사·모바일 메뉴).

## 주요 결정 (배경)
- 제품명 **에이전트 팀 빌더**, 하우스 브랜드 **AI ROASTING**(로스팅은 제품명이 아닌 품질 혈통).
- 5color(판단 레이어)와 별개의 **실행 레이어**. /goal(끈기 루프)과는 보완 관계.
- 단위 둘: 부품(역할 50) vs 레시피(워크플로우). 핵심 IP는 라우터.
- 전달: 정적 카탈로그 사이트(현재) → 검증 후 통합 스킬/소수 스킬 승격(후속).
